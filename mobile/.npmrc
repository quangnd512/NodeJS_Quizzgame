# Bat buoc cho phan mobile.
#
# `react-native@0.86.0` khai bao peerOptional `@react-native/jest-preset@0.86.0`,
# trong khi `jest-expo@57` yeu cau `^0.86.3` — hai rang buoc khong the thoa man cung luc.
# Khong co dong duoi day thi `npm ci` tren CI se that bai voi loi ERESOLVE.
#
# An toan vi day chi la xung dot version cua PRESET DUNG KHI TEST, khong anh huong
# code chay that cua app.
legacy-peer-deps=true
