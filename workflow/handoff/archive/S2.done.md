[TỪ S8-GIAMSAT]

↩️ YÊU CẦU LÀM LẠI: Mobile Stage 1 (feature/mobile-stage1)
🌿 BRANCH: feature/mobile-stage1

⚠️ 2 BUG CẦN FIX (S8 xác minh độc lập bằng code):

---

BUG 1 — TypeScript error (BLOCKER)
File: mobile/src/screens/practice/PracticeSessionScreen.tsx dòng 57
Lỗi: `let countdownInterval: NodeJS.Timeout | undefined;`
→ `NodeJS.Timeout` không hợp lệ trong React Native/Expo
→ `npm run typecheck` báo: error TS2694: Namespace 'global.NodeJS' has no exported member 'Timeout'
Fix: đổi thành `ReturnType<typeof setInterval> | undefined`

---

BUG 2 — Battle answer display logic sai (M13)
File: mobile/src/screens/battle/BattleSessionScreen.tsx dòng 146–158 (hàm getOptStyle)
Vấn đề: `const isCorrect = i === qResult.correctAnswer;`
→ `correctAnswer` có type `number | null` — nếu backend gửi `null`, mọi option đều hiện đỏ dù user trả lời đúng
→ Trong battleSocket.ts: `BattleQuestionResultEvent.correctAnswer: number | null`
Fix: kết hợp 2 field:
  - User trả lời đúng: `qResult.isCorrect && i === selectedOpt` → highlight XANH
  - Đáp án đúng (khi server gửi): `qResult.correctAnswer !== null && i === qResult.correctAnswer` → highlight XANH
  - User chọn sai: `i === selectedOpt && !qResult.isCorrect` → highlight ĐỎ

---

THÊM (không blocking nhưng fix luôn được):
BUG 3 — M12 Battle UI có thể hiện NaN
Kiểm tra trong BattleSessionScreen: state myScore/oppScore init 0 là đúng,
nhưng xem lại BattleResultScreen có guard `|| 0` chưa khi nhận route.params.
Nếu chưa, thêm guard.

---

👉 Sau khi fix:
1. npm run typecheck → PASS (0 error)
2. npm test → 66/66 PASS
3. npm run lint → PASS
4. Ghi kết quả vào workflow/handoff/PENDING/S8.md
5. Thông báo người dùng để chuyển lại S8

📄 Chi tiết test thủ công: workflow/handoff/archive/S5.done.md
