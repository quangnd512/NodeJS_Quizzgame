[TỪ S1-KIENTRUCSU]

🎯 NHIỆM VỤ: Tách App.tsx — VÒNG 1/7 (nợ kỹ thuật)
🌿 BRANCH: refactor/split-app-tsx-round-1
📄 Kế hoạch đầy đủ 9 vòng: workflow/handoff/backlog/S1_tech_debt_master_plan.md

📝 BỐI CẢNH:
frontend/src/App.tsx hiện có 7.018 dòng — toàn bộ giao diện web nằm trong 1 file.
Vòng 1 tách ra phần đơn giản nhất: component dùng chung + màn auth.
KHÔNG đổi bất kỳ hành vi, CSS, hay logic nào — chỉ di chuyển code.

📋 DANH SÁCH TASK:

TASK 1: [frontend] Tạo frontend/src/lib/constants.ts
  → export const SUBJECTS = [...] (copy 9 môn từ App.tsx dòng 49-63)
  → export const SUBJECTS_MAP: Record<string, {name, emoji}> (copy từ App.tsx dòng 920-931)
  → Trong App.tsx: xóa 2 khai báo trên, thêm import từ './lib/constants'
  → Phụ thuộc: không

TASK 2: [frontend] Tạo frontend/src/components/Spinner.tsx
  → Copy hàm Spinner() từ App.tsx dòng 2344-2347
  → Xóa định nghĩa gốc khỏi App.tsx, thêm import
  → Phụ thuộc: không

TASK 3: [frontend] Tạo frontend/src/components/GoogleIcon.tsx
  → Copy hàm GoogleIcon() từ App.tsx dòng 2348-2360
  → Xóa định nghĩa gốc khỏi App.tsx, thêm import
  → Phụ thuộc: không

TASK 4: [frontend] Tạo frontend/src/components/AvatarCell.tsx
  → Copy hàm AvatarCell() từ App.tsx dòng 2103-2115 (kèm cả colors array nội bộ)
  → Xóa định nghĩa gốc khỏi App.tsx, thêm import
  → Phụ thuộc: không

TASK 5: [frontend] Tạo frontend/src/screens/LoadingScreen.tsx
  → Copy hàm LoadingScreen() từ App.tsx dòng 397-406
  → Xóa định nghĩa gốc khỏi App.tsx, thêm import
  → Phụ thuộc: không

TASK 6: [frontend] Tạo frontend/src/screens/LoginPage.tsx
  → Copy hàm LoginPage() từ App.tsx dòng 408-451
  → Import cần: { useState } từ react, { signInWithPopup } từ firebase/auth,
    { firebaseAuth, googleProvider } từ ../lib/firebase,
    Spinner từ ../components/Spinner, GoogleIcon từ ../components/GoogleIcon
  → Props: { onError: (m: string) => void }
  → Xóa định nghĩa gốc khỏi App.tsx, thêm import
  → Phụ thuộc: TASK 2, 3

TASK 7: [frontend] Tạo frontend/src/screens/OnboardingPage.tsx
  → Copy hàm OnboardingPage() từ App.tsx dòng 452-527
  → Import cần: { useState } từ react, { updateSubjects } từ ../lib/api,
    Spinner từ ../components/Spinner, SUBJECTS từ ../lib/constants
  → Props: { sessionToken, currentSubjects, onDone, onError }
  → Xóa định nghĩa gốc khỏi App.tsx, thêm import
  → Phụ thuộc: TASK 1, 2

TASK 8: [frontend] Viết test cho các file vừa tách
  → frontend/src/components/__tests__/Spinner.test.tsx
      - render không crash
      - có aria-hidden="true"
  → frontend/src/components/__tests__/AvatarCell.test.tsx
      - hiện <img> khi có avatarUrl
      - hiện initials khi không có avatarUrl
      - size mặc định 40, có thể override
  → frontend/src/screens/__tests__/LoadingScreen.test.tsx
      - render text "Đang kết nối…"
  → frontend/src/screens/__tests__/LoginPage.test.tsx
      - mock signInWithPopup (vi.mock('firebase/auth'))
      - render nút "Đăng nhập bằng Google"
      - click nút → signInWithPopup được gọi
  → frontend/src/screens/__tests__/OnboardingPage.test.tsx
      - render đủ 9 môn
      - click môn → chọn được (class 'on')
      - không chọn quá 7 môn
      - submit → updateSubjects được gọi với đúng danh sách
  → Phụ thuộc: TASK 2-7

📋 DEFINITION OF DONE:
□ Tồn tại: frontend/src/lib/constants.ts (SUBJECTS, SUBJECTS_MAP)
□ Tồn tại: frontend/src/components/Spinner.tsx, GoogleIcon.tsx, AvatarCell.tsx
□ Tồn tại: frontend/src/screens/LoadingScreen.tsx, LoginPage.tsx, OnboardingPage.tsx
□ App.tsx KHÔNG còn định nghĩa 6 component và 2 hằng số trên (chỉ còn import)
□ App.tsx ≤ 6.600 dòng (từ 7.018)
□ npm run build frontend PASS
□ npm run lint frontend PASS
□ npm test frontend PASS (test cũ vẫn xanh + ≥ 5 test file mới)
□ S5 hồi quy thủ công: đăng nhập Google → chọn môn → vào màn hình chính

🚫 NGOÀI PHẠM VI:
- KHÔNG tách LeaderboardPage, ProfilePage hay bất kỳ màn hình nào khác
- KHÔNG sửa CSS, logic, behavior của bất kỳ component nào
- SUBJECTS_MAP đã chuyển vào constants.ts, nhưng TREND_ICON/TREND_COLOR/DIFF_LABEL
  và các hằng số khác vẫn giữ trong App.tsx (chưa tới lượt)

⚠️ LƯU Ý:
- Sau khi tách, TypeScript import path phải dùng extension .js
  (VD: import Spinner from '../components/Spinner.js') vì project dùng NodeNext module
- Chạy npm run build ngay sau mỗi TASK để phát hiện lỗi import sớm

✅ YÊU CẦU SAU KHI XONG:
- Báo App.tsx còn bao nhiêu dòng (mục tiêu ≤ 6.600)
- Báo S3 review
