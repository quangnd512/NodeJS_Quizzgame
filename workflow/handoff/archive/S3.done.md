[S3-SOATLOI] ✅ HOÀN THÀNH — Mobile Complete bug fixes

📌 Branch: feature/mobile-complete
📌 Commits: abc7e55, 42956d1

## Đã làm:
- ExamListScreen: alertShown useState → useRef (fix stale closure bug)
- ExamListScreen: thêm handleResumeExam() gọi GET /api/exam/resume
- PracticeSessionScreen: handleAutoComplete → useCallback (fix lint error), timer 30' countdown
- ExamTakingScreen: setSubmitting(false) trước navigate (review S5 OK)
- ExamListScreen: disable chip khi không có đề, timeout auto-reset (review S5 OK)
- backend: thêm GET /api/exam/resume + resumeSession() service method
- mobile/api/exam: thêm resumeExam() function
- Xóa 21 file duplicate (có khoảng trắng trong tên)
- Stage + delete PENDING/S2.md

## Kết quả kiểm tra:
- typecheck mobile: PASS
- lint mobile: 0 errors, 5 warnings (acceptable)
- test mobile: 30/30 PASS
- typecheck backend: PASS

## Lỗi S5 phát hiện đã sửa:
1. Stale closure alertShown (CRITICAL) → useRef
2. navigate ExamTaking với ActiveExamSession (type mismatch) → thêm resume API
3. PracticeSessionScreen lint error (hoisted function dùng trước khai báo) → useCallback

## Ghi chú:
- S5 không nên sửa code trực tiếp — từ nay S5 chỉ báo bug, S3 sửa
- Backend endpoint GET /api/exam/resume là thêm mới để hỗ trợ resume flow

📄 Chi tiết: workflow/handoff/archive/S3.done.md (file này)
