[TỪ S8-GIAMSAT]

↩️ YÊU CẦU LÀM LẠI: Thi đấu đối kháng — PvP Quiz Battle (Feature 016, ĐỢT 1/MVP)
🌿 BRANCH: feature/battle-mvp

📋 BỐI CẢNH: S8 đã tự kiểm chứng độc lập toàn bộ vòng này: tsc sạch (BE+FE), 276/276 test
backend PASS, npm audit BE/FE chỉ có lỗ hổng pre-existing (đã xác nhận qua `npm ls` — không
liên quan socket.io/socket.io-client). Đọc kỹ battle.engine.service.ts và
battle.match.service.ts: 6 lỗi S3 đã sửa ở vòng review trước (atomic transaction, CAS chống
trả điểm 2 lần, bug lịch sử dùng winnerId thay vì so sánh điểm) đều đúng, code chất lượng tốt.

⚠️ VẤN ĐỀ: Thiếu test tự động cho tầng điều phối realtime (battle.engine.service.ts) —
vi phạm 1 dòng DoD của S1.

DoD gốc của S1 (trong PENDING/S2.md) ghi rõ:
"có test mới cho: hàm tính điểm tốc độ, logic ghép trận nới lỏng theo mốc thời gian, 4 kịch
bản thanh toán điểm (thắng người thật/thua/hoà/thắng bot), xử lý mất kết nối 30 giây (CẢ 2
NHÁNH: reconnect kịp / không kịp)"

Thực tế: `backend/src/services/battle/__tests__/` chỉ có 3 file — battle.utils.test.ts,
battle.queue.service.test.ts, battle.match.service.test.ts. KHÔNG có
battle.engine.service.test.ts nào cả. Test hiện có trong battle.match.service.test.ts cho
DISCONNECT_WIN/CANCELLED (dòng 300, 317) CHỈ test hàm settleMatch() khi outcome đã được xác
định sẵn (test gọi thẳng settleMatch({ outcome: {type:'DISCONNECT_WIN', ...} })) — tức là
chỉ test tầng THANH TOÁN ĐIỂM sau khi đã biết kết quả, KHÔNG test tầng QUYẾT ĐỊNH khi nào/có
xảy ra outcome đó hay không — đó chính là logic nằm trong battle.engine.service.ts:
- `onPlayerDisconnected` / `handleDisconnectGraceExpired` (đếm 30s, xử thắng kỹ thuật)
- `attemptReconnectToActiveMatch` (gán lại socket, huỷ đếm 30s, KHỞI ĐỘNG LẠI questionTimer)
- Sự phối hợp giữa `questionTimer` (20.5s/câu) và `disconnectTimers` (30s mất kết nối) —
  đây CHÍNH XÁC là chỗ xảy ra bug #9 mà S5 tìm thấy bằng tay (2 đồng hồ chạy độc lập khiến
  trận tự "nhảy câu" trước khi đủ 30s chờ) và bug #10 (chặn 2 trận cùng lúc, hàm
  `hasActiveMatch()`) — cả 2 fix này hiện CHỈ được xác minh THỦ CÔNG (S5 dùng script kết nối
  Socket.io thật), KHÔNG có test tự động nào bảo vệ lâu dài. Nếu sau này có ai vô tình sửa
  lại `sendNextQuestion`/`onPlayerDisconnected` mà làm hỏng lại đúng bug này, sẽ không có
  test nào báo đỏ.

Cả S5 và S6 đều đã tự ghi chú rõ đây là rủi ro cao nhất còn tồn đọng trong báo cáo bàn giao
của mình — S8 xác nhận đúng là vậy sau khi tự đọc code, nên trả lại để bổ sung trước khi cho
merge.

👉 Yêu cầu: Viết thêm `backend/src/services/battle/__tests__/battle.engine.service.test.ts`
(hoặc file tương đương phù hợp cấu trúc export hiện có — có thể cần export thêm vài hàm
nội bộ hoặc dùng cách mock Namespace/socket giả lập giống các test socket khác trong dự án
nếu có, hoặc tách nhỏ phần logic thuần túy dễ test ra khỏi phần I/O socket nếu thấy hợp lý
hơn — tuỳ đánh giá kỹ thuật của S3). Tối thiểu cần phủ:
1. Mất kết nối 1 bên → chờ đúng 30s không quay lại → xử thắng kỹ thuật cho bên còn lại
   (winnerIsPlayer1 đúng chiều)
2. Mất kết nối 1 bên → quay lại (reconnect) TRƯỚC 30s → KHÔNG xử thua, trận tiếp tục, điểm
   số không bị mất, questionTimer được khởi động lại đúng (không bị "treo" mãi mãi)
3. questionTimer bị tạm dừng đúng lúc `onPlayerDisconnected` (không tự động nhảy câu trong
   lúc đang chờ 30s) — đây là bug #9 cụ thể S5 đã tìm thấy
4. Cả 2 bên cùng mất kết nối trong lúc chờ → huỷ trận ngay (CANCELLED), không chờ hết 30s
   của bên thứ 2
5. `hasActiveMatch()` chặn đúng khi user đã có 1 trận đang diễn ra (không cho vào hàng đợi/
   tạo phòng/vào phòng lần 2) — bug #10 cụ thể S5 đã tìm thấy

CHỈ cần bổ sung test — KHÔNG cần sửa lại logic (logic hiện tại đã đúng theo S5 xác nhận thủ
công). Nếu trong lúc viết test phát hiện thêm bug thật thì báo rõ và sửa luôn.

Sau khi xong:
- Ghi kết quả (test mới, số lượng, PASS) vào workflow/handoff/PENDING/S8.md
- Thông báo người dùng để chuyển lại cho S8 rà soát lại
