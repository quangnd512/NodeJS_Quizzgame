[TỪ S3-SOATLOI]

✅ ĐÃ LÀM LẠI XONG yêu cầu của S8 (xem workflow/handoff/PENDING/S3.md → đã đổi tên S3.done.md).

📋 VIỆC ĐÃ LÀM:
Viết mới `backend/src/services/battle/__tests__/battle.engine.service.test.ts` (6 test) cho
tầng điều phối realtime `battle.engine.service.ts` — trước đây HOÀN TOÀN không có test tự
động (module chỉ export `registerBattleEventHandlers` + `getActiveMatchSnapshot`, state nằm
private). Giả lập tối thiểu `Namespace`/`Socket` Socket.io (chỉ đủ field
`id`/`data.user`/`on`/`emit`/`join` mà code thực sự dùng) + `vi.useFakeTimers()` /
`vi.advanceTimersByTimeAsync()` để điều khiển chính xác mốc 30s (disconnect grace) và 20.5s
(question timer) mà không cần chờ thật. Mock hoàn toàn `battle.match.service.js` (đã có test
riêng ở vòng trước) + `prisma.user.findUnique` + `pointsService.getBalance`. Dùng thẳng
`battleQueueService` thật (không mock) qua luồng tạo-phòng/vào-phòng để dựng 1 trận thật,
tránh phải điều khiển vòng lặp hàng đợi 1 giây.

KHÔNG sửa lại logic (logic hiện tại đúng theo S5 đã xác nhận thủ công) — chỉ bổ sung test.
Trong lúc viết, KHÔNG phát hiện bug thật nào mới.

🧪 PHỦ ĐỦ 5 KỊCH BẢN S8 YÊU CẦU (6 test, tất cả PASS ngay lần chạy đầu):
1. Mất kết nối 1 bên, KHÔNG reconnect trong 30s → xử thắng kỹ thuật, đúng chiều
   `winnerIsPlayer1` (test riêng cả 2 chiều: player1 mất kết nối và player2 mất kết nối)
2. Mất kết nối rồi reconnect TRƯỚC 30s → KHÔNG xử thua, điểm giữ nguyên (assert chính xác
   13đ = 10 cơ bản + 3 bonus tốc độ tối đa), disconnect timer bị huỷ THẬT (chờ thêm 30s sau
   reconnect vẫn không settle — không chỉ "trì hoãn"), questionTimer khởi động lại đúng
   (chờ 20.5s kể từ lúc reconnect thấy câu tiếp theo được gửi — không bị "treo mãi mãi")
3. Bug #9 (S5 tìm bằng tay): trong lúc chờ 30s, vượt mốc 20.5s cũ KHÔNG tự "nhảy câu"
4. Cả 2 cùng mất kết nối → huỷ trận (CANCELLED) NGAY LẬP TỨC, không đợi hết 30s của bên thứ
   2, và không settle 2 lần khi thời gian trôi hết
5. Bug #10 (S5 tìm bằng tay): `hasActiveMatch()` chặn đúng `join-queue`/`create-room`/
   `join-room` khi user đã có 1 trận đang diễn ra (kể cả từ tab/socket thứ 2) →
   `BATTLE_ALREADY_IN_MATCH`, không tạo thêm trận nào

📊 KẾT QUẢ KIỂM CHỨNG ĐỘC LẬP:
- Test mới: 6/6 PASS
- Toàn bộ backend: 282/282 PASS (276 cũ + 6 mới)
- Build: `npx tsc --noEmit` PASS (sạch, không có lỗi TypeScript)
- Lint: backend không có cấu hình eslint (xác nhận lại từ vòng review trước — không phải
  thiếu sót của lần làm lại này, không đụng tới frontend)
- npm audit: KHÔNG có thay đổi dependency nào trong lần làm lại này (chỉ thêm 1 file test) →
  giữ nguyên kết luận audit của S8 ở vòng trước (chỉ lỗ hổng pre-existing, không liên quan
  socket.io/socket.io-client)

📁 FILES ĐÃ THÊM/SỬA:
- backend/src/services/battle/__tests__/battle.engine.service.test.ts (MỚI, 6 test)
- docs/TEST_CASES.md (cập nhật ghi chú đầu mục Feature 016 dòng test tự động; đánh dấu case
  #16, #28 nay có test tự động; thêm case #29 — bug #9, #30 — bug #10)
- docs/CODE_REVIEW_LOG.md (thêm mục "LÀM LẠI lần 1: bổ sung test tự động cho tầng điều phối
  realtime")

👉 Nhờ S8 rà soát lại. S4 vẫn đang làm song song phần docs (không liên quan tới thay đổi này).

---

[TỪ S4-GHICHEP]

✅ ĐÃ LÀM LẠI XONG yêu cầu của S8 (xem workflow/handoff/PENDING/S4.md → đã đổi tên S4.done.md).
Làm độc lập, song song với S3 ở trên — không đụng vào file test/code, chỉ sửa docs.

📋 BỐI CẢNH: S8 xác nhận docs chưa cập nhật theo 3 thay đổi phạm vi muộn phát sinh trong vòng
test thủ công của S5: (1) ẩn hoàn toàn danh tính bot ("Máy 🤖" → tên giả kiểu người thật), (2)
endpoint mới `GET /api/battle/active` (tự động vào lại trận đang dở), (3) mã lỗi mới
`BATTLE_ALREADY_IN_MATCH` (chặn 1 user vào 2 trận cùng lúc).

📄 VIỆC ĐÃ LÀM (4 file S8 yêu cầu):
1. **docs/FEATURE_LOG.md** (Section 16): thêm mục kỹ thuật #8 (bug #9 pause/resume
   questionTimer trong lúc chờ mất kết nối, bug #10 `BATTLE_ALREADY_IN_MATCH`) và #9 (ẩn danh
   tính bot, tự động resume trận đang dở) — viết đầy đủ vấn đề + giải pháp theo đúng phong
   cách các mục 1-7 đã có. Sửa sơ đồ ASCII A (bot text) + D (bổ sung pause timer), thêm sơ đồ
   E mới (luồng GET /api/battle/active). Cập nhật Data Model (ghi chú bot ẩn danh + phân biệt
   backend-restart vs F5-thường), API Reference (thêm bảng + subsection `GET /api/battle/active`
   đầy đủ ví dụ, sửa ví dụ JSON `opponentName` không còn null, thêm dòng bảng mã lỗi
   `BATTLE_ALREADY_IN_MATCH`), file structure (đếm lại đúng 14 lớp lỗi, 81 test tự động), 2
   bước test thủ công mới (#9 auto-resume 2 nhánh, #10 chặn 2 trận).
2. **docs/guides/user-guide.md**: bỏ toàn bộ "Máy 🤖", thêm ghi chú đầu mục giải thích UI ẩn
   danh tính bot có chủ đích (tài liệu vẫn giải thích sự thật cho dev/tester). Cập nhật màn
   chờ ghép trận, đếm ngược mất kết nối (theo giây thật), tự động resume, màn lịch sử. Thêm 2
   FAQ mới (auto-resume, chặn 2 trận `BATTLE_ALREADY_IN_MATCH`).
3. **docs/api/drafts/battle-mvp.yaml**: thêm endpoint `GET /api/battle/active` đầy đủ, mã lỗi
   `BATTLE_ALREADY_IN_MATCH` ở 3 event liên quan, sửa `opponentName` (không còn null/"Máy" cho
   bot), ghi chú pause-timer ở `battle:opponent-disconnected`, 3 note kỹ thuật mới cuối file.
   Lưu ý: file draft này của S1 vốn KHÔNG phải strict YAML (dùng cú pháp rút gọn `number?`,
   `string | null` — xác nhận đây là style nhất quán với các draft khác trong dự án, không
   phải lỗi tôi gây ra).
4. **docs/TEST_CASES.md**: thêm subsection mới "Thay đổi phạm vi muộn — xác nhận thủ công bởi
   S5" với 5 kịch bản B1-B5 (ẩn danh tính bot × 2, auto-resume 2 nhánh, đếm ngược mất kết nối)
   — theo đúng convention N1-N4 đã dùng ở Feature 013. KHÔNG đụng vào #29/#30 mà S3 đã viết
   song song (đọc trước khi sửa, chỉ append thêm).

📄 VIỆC LÀM THÊM (không nằm trong 4 file S8 liệt kê, nhưng cùng bị lỗi thời tương tự — tự phát
hiện khi rà lại toàn bộ, thấy nếu không sửa sẽ bị S8 bắt lại ở vòng sau):
- **docs/api/openapi.yaml** (hợp đồng API chính thức, khác file draft ở trên): bump
  v1.7.0→v1.7.1, thêm path `/api/battle/active` + 2 schema mới (`ActiveBattleMatchSnapshot`,
  `ActiveBattleMatchResponse`), sửa toàn bộ ví dụ/mô tả `opponentName` (bỏ `nullable`, bỏ ví dụ
  "Máy"), thêm mã lỗi `BATTLE_ALREADY_IN_MATCH` vào description các event liên quan. Đã
  `python3 -c "import yaml; yaml.safe_load(...)"` xác nhận YAML hợp lệ sau khi sửa.
- **docs/guides/admin-guide.md**: thêm cách dùng `GET /api/battle/active` để admin/support tra
  cứu trận đang "sống" (nhanh hơn SQL vì `battle_matches` không cập nhật realtime lúc
  IN_PROGRESS), ghi chú `isBotMatch` trong DB vẫn luôn đúng dù UI đã ẩn, làm rõ phân biệt
  backend-restart-thật vs F5-thường ở mục giới hạn hạ tầng, 1 mục xử lý sự cố mới về
  `BATTLE_ALREADY_IN_MATCH`.
- **docs/guides/troubleshooting.md**: làm rõ mục #28 (thêm cảnh báo: nếu chỉ F5 thường mà vẫn
  mất trận thì nay là BUG THẬT, không còn là hành vi chấp nhận được), thêm mục #30
  (`BATTLE_ALREADY_IN_MATCH`) và #31 (đối thủ tên lạ trong lịch sử = có thể là bot, không phải
  lỗi dữ liệu).
- **docs/CHANGELOG.md**: cập nhật entry Feature 016 hiện có — thêm mục Fixed thứ 2 (2 bug S5
  tìm bằng tay) và mục Changed mới (3 thay đổi phạm vi), cập nhật mục Docs.

✅ KIỂM CHỨNG ĐÃ TỰ LÀM (không chỉ tin mình viết đúng):
- `grep -rn "Máy\|🤖"` toàn bộ docs/ liên quan Battle — xác nhận KHÔNG còn claim nào nói UI
  hiện "Máy"/🤖 (chỉ còn các câu giải thích lịch sử/chú thích có chủ đích, đọc lại từng dòng
  xác nhận đúng ý nghĩa).
- `grep -rl "BATTLE_ALREADY_IN_MATCH"` và `grep -rl "battle/active"` — xác nhận cả 2 xuất hiện
  nhất quán ở toàn bộ file liên quan (FEATURE_LOG, TEST_CASES, 2 file API contract, 2 guide,
  troubleshooting, CHANGELOG).
- YAML `docs/api/openapi.yaml` parse thành công bằng `pyyaml` sau khi sửa.
- Đối chiếu trực tiếp với code thật trước khi viết (không đoán): đọc `battle.utils.ts`
  (`pickBotDisplayName`, `BOT_DISPLAY_NAMES`), `battle.engine.service.ts`
  (`hasActiveMatch`, `getActiveMatchSnapshot`, vị trí chính xác của check trong 3 handler,
  logic pause/resume `questionTimer`), `battle.types.ts` (`ActiveBattleMatchSnapshot`), `battle.route.ts`,
  `battle.errors.ts`, `frontend/src/App.tsx` (luồng `BattleResumeState`,
  `BATTLE_ACTIVE_MATCH_KEY` trong localStorage) và `frontend/src/lib/api.ts`
  (`getActiveBattleMatch`) — không dựa vào suy đoán từ báo cáo S5 mà tự đọc code xác nhận.

📁 TỔNG HỢP FILES ĐÃ SỬA (8 file):
- docs/FEATURE_LOG.md, docs/TEST_CASES.md, docs/api/drafts/battle-mvp.yaml,
  docs/guides/user-guide.md (4 file S8 yêu cầu)
- docs/api/openapi.yaml, docs/guides/admin-guide.md, docs/guides/troubleshooting.md,
  docs/CHANGELOG.md (bonus, tự phát hiện)

👉 Nhờ S8 rà soát lại.
