[TỪ S8-GIAMSAT]

↩️ YÊU CẦU LÀM LẠI: Thi đấu đối kháng — PvP Quiz Battle (Feature 016, ĐỢT 1/MVP)
🌿 BRANCH: feature/battle-mvp

📋 BỐI CẢNH: S8 đã đối chiếu docs hiện tại với các thay đổi phạm vi phát sinh ở vòng test
của S5 (ẩn hoàn toàn danh tính bot, tự động vào lại trận đang dở, endpoint mới, mã lỗi mới).
Cả S5 và S6 đều đã ghi chú rõ trong báo cáo bàn giao rằng phần docs này CHƯA được cập nhật
lại theo các thay đổi muộn đó và nhờ S8 xác nhận — S8 xác nhận đúng là CHƯA, bằng cách grep
trực tiếp trong docs hiện tại (không chỉ tin báo cáo):

⚠️ VẤN ĐỀ CỤ THỂ:

1. Vẫn còn mô tả bot cũ "Máy 🤖" — đã lỗi thời so với thay đổi ẩn danh tính bot
   (`pickBotDisplayName` trong `battle.utils.ts`, dùng tên giả deterministic theo matchId):
   - `docs/FEATURE_LOG.md` dòng 7709: '...xác nhận tự động ghép với "Máy" (🤖).'
   - `docs/guides/user-guide.md` dòng 1491: '...hệ thống tự động cho bạn đấu với **Máy 🤖**.'
   → Cần sửa lại: không còn hiện icon 🤖 hay chữ "Máy" ở màn thi đấu/lịch sử, đối thủ bot
   hiện tên giả giống người thật (nhưng có thể ghi chú trong docs nội bộ/admin-guide rằng
   đây là bot, vì `isBotMatch` vẫn có trong dữ liệu — chỉ tầng hiển thị người dùng mới ẩn).

2. Endpoint mới `GET /api/battle/active` (tự động vào lại trận đang dở sau khi tải lại
   trang/đăng nhập lại — đọc `ActiveBattleMatchSnapshot` từ state in-memory) CHƯA có trong
   `docs/api/drafts/battle-mvp.yaml` (đã kiểm tra bằng grep, không có kết quả nào) và cũng
   chưa được mô tả trong `docs/FEATURE_LOG.md` / `docs/guides/user-guide.md`.

3. Mã lỗi mới `BATTLE_ALREADY_IN_MATCH` (từ `BattleAlreadyInMatchError`, chặn user mở 2 kết
   nối cùng lúc vào 2 trận — fix bảo mật S5 tìm thấy) CHƯA xuất hiện ở BẤT KỲ đâu trong thư
   mục docs/ (đã grep toàn bộ, 0 kết quả) — không có trong api draft, không có trong
   troubleshooting.md, không có trong FEATURE_LOG.md.

4. `docs/TEST_CASES.md` (mục "Test Cases: Thi đấu đối kháng...") hiện dừng ở case #28, do
   S3 viết TRƯỚC vòng test của S5 — thiếu đúng 4 kịch bản S5 đã tự liệt kê trong báo cáo bàn
   giao là "chưa có": đếm ngược mất kết nối hiển thị theo giây thật, tự động resume trận
   đang dở (2 nhánh: quay lại kịp trong 30s / quay lại trễ), chặn 2 trận cùng lúc
   (BATTLE_ALREADY_IN_MATCH), ẩn danh tính bot (không còn "Máy"/🤖 ở bất kỳ đâu trên UI).

📌 KHÔNG cần đụng vào: `docs/GLOSSARY.md`, `docs/adr/013-pvp-battle-realtime-state.md`,
`docs/LESSONS_LEARNED.md` — đây là 3 file do S6 tự viết, đã phản ánh đúng các thay đổi muộn
này rồi (S8 đã kiểm tra, đúng).

👉 Yêu cầu: Cập nhật 4 file trên (FEATURE_LOG.md, user-guide.md, api/drafts/battle-mvp.yaml,
TEST_CASES.md) cho khớp đúng trạng thái code hiện tại. Tiện thể rà lại troubleshooting.md và
admin-guide.md xem có chỗ nào khác còn nhắc "Máy 🤖" hoặc thiếu 2 mục mới (endpoint + mã lỗi)
mà S8 grep sơ bộ có thể đã bỏ sót.

Khi sửa xong:
- Ghi kết quả (danh sách file đã sửa, tóm tắt) vào workflow/handoff/PENDING/S8.md
- Thông báo người dùng để chuyển lại cho S8 rà soát lại

⚠️ LƯU Ý: S3 cũng đang được yêu cầu làm lại song song (bổ sung test cho
battle.engine.service.ts) — không liên quan đến phần việc của S4, không cần chờ nhau, cả 2
có thể làm độc lập cùng lúc.
