[TỪ S4-GHICHEP]

[S4-GhiChep] ✅ TÀI LIỆU XONG: Thi đấu đối kháng — PvP Quiz Battle (Feature 016, ĐỢT 1/MVP)
🌿 BRANCH: feature/battle-mvp

📄 TÀI LIỆU ĐÃ CẬP NHẬT:
- FEATURE_LOG.md: Section 16 mới — tổng quan, 7 mục kỹ thuật (giải thích + lý do 6 lỗi S3 sửa), sơ đồ ASCII 4 luồng (ghép trận / vòng lặp câu hỏi / kết thúc + thanh toán / mất kết nối), data model, API reference đầy đủ, file structure, cách tự kiểm thử, lưu ý/TODO
- openapi.yaml: có — bump lên v1.7.0, thêm tag `Battle`, 2 REST endpoint (`GET /api/battle/config`, `GET /api/battle/history`) với ví dụ thực tế, 4 schema mới (`BattleConfig`, `BattleResult`, `BattleHistoryItem`, `PaginatedBattleHistory`), và extension `x-socketio-namespaces` mô tả đầy đủ toàn bộ 5 sự kiện client→server + 8 sự kiện server→client (OpenAPI 3.0 không có cú pháp chuẩn cho Socket.io) — đã validate YAML hợp lệ
- admin-guide.md: có — mục 16 mới (không cần cấu hình gì thêm, cách theo dõi trận qua SQL, đối soát điểm cược escrow, giới hạn hạ tầng 1-instance, 4 sự cố thường gặp)
- user-guide.md: có — mục mới đầy đủ cách chơi (vào trận, chờ ghép, thi đấu, kết quả, lịch sử) + FAQ
- CHANGELOG.md: có — entry `[Unreleased] — Feature 016` mới ở đầu file (Added/Fixed/Docs)
- troubleshooting.md: có — 5 mục mới #25-29 (nút không phản ứng do socket chưa kết nối, số dư hiển thị cũ, banner mất kết nối chậm, mất trận khi restart backend, lỗi vào phòng bằng mã)
- README.md: không — không có README.md cấp dự án (chỉ có template Vite mặc định ở frontend/README.md)

📌 Bối cảnh nghiệp vụ quan trọng cần biết khi test:
- Đây là LẦN ĐẦU dự án dùng kết nối realtime (Socket.io namespace /battle) — ưu tiên test kỹ luồng mất kết nối/reconnect (chờ 30s, xử thắng kỹ thuật, hoàn cược khi cả 2 cùng mất kết nối)
- Cơ chế thanh toán điểm là "khoá cược rồi chia lại" (escrow) — nên test đối soát điểm qua bảng point_transactions (reason PVP_LOCK_BET/PVP_WIN/PVP_DRAW_REFUND/PVP_CANCELLED_REFUND) sau mỗi trận
- Chỉ chạy đúng với 1 instance backend — không cần test đa-instance
- Cần có sẵn ít nhất 10 câu MCQ_4 active mỗi môn muốn test trong Ngân hàng câu hỏi, nếu không sẽ gặp lỗi BATTLE_NOT_ENOUGH_QUESTIONS
- Danh sách 28 test case nghiệp vụ chi tiết đã có sẵn trong docs/TEST_CASES.md (mục "Test Cases: Thi đấu đối kháng")
- Cách tự kiểm thử thủ công (8 bước) đã liệt kê trong docs/FEATURE_LOG.md Section 16

👉 Yêu cầu: Đưa ra danh sách kiểm thử thực tế và hướng dẫn test.
