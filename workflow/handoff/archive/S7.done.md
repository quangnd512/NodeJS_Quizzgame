[TỪ S8-GIAMSAT]

✅ ĐẠT QUALITY GATE: Thi đấu đối kháng — PvP Quiz Battle (Feature 016, ĐỢT 1/MVP)
🌿 BRANCH: feature/battle-mvp

📋 TÓM TẮT: 16/16 TASK hoàn thành đúng yêu cầu (bao gồm 1 vòng làm lại: S3 bổ sung test cho
battle.engine.service.ts, S4 cập nhật docs theo thay đổi phạm vi muộn — cả 2 đã ĐẠT). Test
tự động 282/282 PASS, test thủ công 22/22 PASS. tsc BE+FE sạch. npm audit BE/FE chỉ lỗ hổng
pre-existing, không liên quan socket.io/socket.io-client mới thêm. Tài liệu đầy đủ (FEATURE_LOG,
TEST_CASES, GLOSSARY, CHANGELOG, openapi.yaml + api draft, user-guide/admin-guide/troubleshooting,
ADR-013, LESSONS_LEARNED, GAMEPLAY.md mới tạo).

⚠️ GHI CHÚ QUAN TRỌNG CHO S7 (đọc trước khi push):

1. **RẤT NHIỀU thay đổi qua nhiều vòng (S2 gốc + S3 lần 1 + S5 + S6 + S3 lần 2 + S4 lần 2)
   vẫn CHƯA COMMIT** trong working tree — chỉ có 5 commit gốc của S2 (TASK 1-16) đã lên branch,
   phần còn lại (review fixes, 11 bug S5 sửa, docs, ADR, ~90 test mới) đều là working tree
   changes chưa commit. Cân nhắc cách gộp commit hợp lý (S8 không chỉ định cách chia — tuỳ S7
   quyết định theo convention dự án).

2. **Môi trường cục bộ**: trong lúc QA, tôi phát hiện `node_modules/.bin/tsc` và
   `node_modules/.bin/eslint` bên frontend bị thiếu (cài đặt dở dang — nghi do nhiều session
   Claude cùng chạy `npm install` chung 1 thư mục/node_modules). Hậu quả: `npm run build` từng
   âm thầm rơi vào 1 bản `tsc` global cũ (5.3.3 tại /usr/local/bin/tsc) và báo lỗi sai. Tôi đã
   `npm rebuild` để sửa — hiện `npm run build` và `npm run lint` frontend chạy đúng, sạch (lint:
   0 lỗi, 1 warning không chặn). Nếu S7 gặp lại lỗi build/lint kỳ lạ không liên quan code, thử
   `npm rebuild` trong `frontend/` trước khi nghi ngờ code.

3. File mới `backend/src/services/battle/__tests__/battle.engine.service.test.ts` +
   `docs/adr/013-pvp-battle-realtime-state.md` + `docs/api/drafts/battle-mvp.yaml` đang ở dạng
   untracked (`??` trong git status) — nhớ `git add` đầy đủ, đừng bỏ sót.

👉 Yêu cầu: Push branch lên GitHub, kiểm tra CI pass, hỏi người dùng merge.
