[TỪ S4-GHICHEP]

[S4-GhiChep] ✅ TÀI LIỆU XONG: Quản lý câu hỏi — Học sinh đóng góp câu hỏi + Thiết kế lại luồng xử lý báo cáo câu hỏi (Feature 014)
🌿 BRANCH: feature/question-management-hub

📄 TÀI LIỆU ĐÃ CẬP NHẬT:
- FEATURE_LOG.md: Section 14 mới — tổng quan, 6 phương án kỹ thuật + lý do, data model 2 bảng
  mới (student_question_submissions, question_edit_history), API reference đầy đủ, 3 sơ đồ ASCII
  luồng chạy (approve, usage-points CAS, resolve report batch), file structure, ghi chú kỹ thuật
- openapi.yaml: có — bump 1.4.0→1.5.0; viết lại toàn bộ /api/admin/questions/reports* (JOIN nội
  dung, filter subject/reason, endpoint resolve mới thay PATCH .../:id cũ); thêm 7 endpoint
  Submissions mới + schema liên quan; đã validate YAML + $ref hợp lệ
- admin-guide.md: có — viết lại mục 4 (luồng resolve mới, đặc biệt thay đổi hành vi auto-hide),
  cập nhật mục 5.3/5.5 (tab UI mới "Câu hỏi" gộp Submissions+Reports), cập nhật bảng loại thông
  báo mục 13, thêm mục 14 mới (hướng dẫn admin duyệt/từ chối, điểm thưởng, troubleshooting riêng)
- user-guide.md: có — thêm 3 loại thông báo mới, thêm mục "Đóng góp câu hỏi (Feature 014)" đầy đủ
- CHANGELOG.md: có — entry [Unreleased] đầy đủ Added/Changed/Fixed, 2 breaking change ghi rõ
- troubleshooting.md: có — 5 lỗi phổ biến mới (#15-19)
- README.md: không tồn tại; PROJECT_OVERVIEW.md (tương đương) do S8 sở hữu, không đụng vào

⚠️ LƯU Ý CHO S5 (điểm cần đặc biệt chú ý khi test thủ công vì đây là chỗ dễ miss):
1. Breaking changes: PATCH /api/admin/questions/reports/:id (cũ) đã bị XOÁ — test UI/API phải
   dùng .../resolve. GET .../summary đổi field pending/reviewed → pendingReports/pendingQuestions.
2. Hành vi auto-hide ĐÃ ĐỔI: resolve với status=FIXED giờ TỰ ĐỘNG set isActive=true nếu câu đang
   bị ẩn (trước đây phải khôi phục thủ công) — cần test rõ cả 2 case FIXED và DISMISSED.
3. Batch resolve: xử lý 1 báo cáo sẽ tự đóng luôn mọi báo cáo PENDING khác cùng questionId — cần
   tạo tình huống 1 câu có ≥2 báo cáo để test batchResolvedCount và việc mỗi người báo cáo nhận
   1 thông báo REPORT_RESOLVED riêng.
4. 3 race condition đã được S3 sửa (claim pattern + CAS) — nên thử test concurrent nếu có thể
   (2 request duyệt cùng lúc 1 submission, 2 lần addFromBank cùng lúc 1 câu) để xác nhận thực tế,
   không chỉ dựa vào 20 unit test đã PASS.
5. Rate limit 5 câu/ngày tính theo UTC (không phải giờ VN) — cần lưu ý khi test biên giờ.
6. Điểm usage tối đa 100đ/câu (20 lần dùng) — nên test rõ trường hợp đạt trần không cộng thêm.
7. Badge tab "Câu hỏi" trên Admin Dashboard KHÔNG polling real-time — chỉ refetch khi đổi tab.

👉 Yêu cầu: Đưa ra danh sách kiểm thử thực tế và hướng dẫn test.
