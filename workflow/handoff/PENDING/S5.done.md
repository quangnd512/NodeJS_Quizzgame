[TỪ S8-GIAMSAT]

↩️ YÊU CẦU LÀM LẠI (bổ sung test, nhỏ): Notifications — Feature 013 / TASKS.md ID 009
🌿 BRANCH: feature/notifications

📋 BỐI CẢNH: Vòng test 26/26 PASS trước đây của bạn được thực hiện TRƯỚC KHI có commit 293b40d
(S2 vừa bổ sung logic điều hướng khi bấm thông báo — sửa lỗi DoD ban đầu bị thiếu). Code đã được
S8 tự kiểm chứng qua đọc diff + build/lint/test tự động (PASS), nhưng CHƯA có ai thực sự bấm thử
trên app đang chạy để xác nhận hành vi điều hướng đúng như mong đợi. Đây đúng là loại lỗ hổng đã
khiến cả S3/S5/S4/S6 bỏ sót lần trước (không ai viết test-case cho "click-to-navigate"), nên S8
muốn chốt chặn bằng một vòng test thủ công ngắn trước khi cho ĐẠT.

👉 YÊU CẦU: Test thủ công 4 kịch bản sau (chỉ cần đúng 4 case này, không cần lặp lại 26 case cũ):

1. Có thông báo STREAK_MILESTONE chưa đọc → bấm vào trong panel 🔔
   Expect: chấm xanh (unread dot) biến mất NGAY + app chuyển sang màn hình Tiến độ học tập (ProgressPage)

2. Có thông báo RANK_UP hoặc RANK_DOWN chưa đọc → bấm vào
   Expect: đánh dấu đã đọc + chuyển sang màn hình Bảng xếp hạng (LeaderboardPage)

3. Có thông báo NEW_EXAM_PAPER chưa đọc → bấm vào
   Expect: đánh dấu đã đọc + chuyển sang màn hình Thi thử (ExamPage — màn hình chọn môn thi bình
   thường, không cần tự chọn sẵn môn)

4. Có thông báo REPORT_RESOLVED chưa đọc → bấm vào
   Expect: CHỈ đánh dấu đã đọc, KHÔNG chuyển màn hình — vẫn ở lại panel/màn hình hiện tại
   (đây là case dễ bị "pass giả" nếu code có bug điều hướng luôn — cần xác nhận kỹ app KHÔNG
   nhảy màn hình)

⚠️ Lưu ý khi setup dữ liệu test: có thể cần tạo thủ công notification trong DB (qua Prisma Studio
hoặc script) nếu không muốn chờ trigger tự nhiên (streak 7 ngày, đổi hạng thật, admin duyệt báo
cáo, bật đề thi mới) — 4 loại notification này đã có sẵn cơ chế trigger thật từ vòng làm trước,
chỉ cần đảm bảo mỗi loại có ít nhất 1 bản ghi isRead=false để bấm thử.

Sau khi 4 case xong, tiện thể bổ sung 4 dòng này vào docs/TEST_CASES.md (mục Feature 013) để lần
sau không bị thiếu coverage nữa — đây cũng là đề xuất của S2 khi báo cáo sửa xong.

👉 Khi test xong:
- Ghi kết quả (PASS/FAIL từng case, bug nếu có) vào workflow/handoff/PENDING/S8.md
- Thông báo người dùng để chuyển lại cho S8 xác nhận ĐẠT quality gate lần cuối.
