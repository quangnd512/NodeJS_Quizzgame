[TỪ S4-GHICHEP]

[S4-GhiChep] ✅ TÀI LIỆU XONG: Khung Free/Premium (Feature 015)
🌿 BRANCH: feature/premium-framework

📄 TÀI LIỆU ĐÃ CẬP NHẬT:
- FEATURE_LOG.md: Section 15 mới — tổng quan, 7 phương án kỹ thuật + lý do, data model đầy đủ,
  API reference, 4 sơ đồ ASCII luồng chạy, file structure, ghi chú kỹ thuật (2 race condition đã sửa)
- openapi.yaml: có — bump 1.5.0→1.6.0; 4 endpoint mới + 3 schema mới; gate 403 cho wrong-answers/
  progress/exam-history. NHÂN TIỆN sửa 1 lỗi cấu trúc có sẵn: 12 endpoint (question-bank,
  leaderboard, avatar, progress, wrong-answers) bị lồng sai vào components: thay vì paths: —
  đã chuyển đúng vị trí + sửa 8 chỗ tham chiếu bearerAuth (không tồn tại) → SessionToken. Verify
  bằng swagger-cli validate: sạch, không lỗi liên quan Feature 015.
- admin-guide.md: có — mục 15 đầy đủ (công tắc toàn cục, cấp Premium + quy tắc cộng dồn, bảng
  quyền lợi, giải thích streak freeze, cron, troubleshooting riêng)
- user-guide.md: có — cập nhật Đổi môn/Tiến độ/Ôn câu sai với gate mới, mục lớn "Free/Premium —
  Quyền lợi tài khoản"
- CHANGELOG.md: có — entry [Unreleased] đầy đủ
- troubleshooting.md: có — 5 lỗi phổ biến mới (#20-24)
- README.md: không tồn tại; PROJECT_OVERVIEW.md do S8 sở hữu, không đụng vào

⚠️ LƯU Ý CHO S5 (điểm cần đặc biệt chú ý khi test thủ công):
1. Công tắc toàn cục "Mặc định Premium cho tất cả" mặc định BẬT SẴN — test cả 2 trạng thái
   BẬT/TẮT, xác nhận cache invalidate ngay (không cần restart server, không có độ trễ).
2. Gate đổi môn học: test đúng thứ tự — xem quảng cáo → ad-unlock → đổi môn thành công; và test
   trường hợp gửi subjects SAI ĐỊNH DẠNG sau khi ad-unlock → xác nhận token KHÔNG bị "đốt" oan
   (đây chính là bug S3 đã sửa, cần re-verify bằng tay trên UI thật, không chỉ tin unit test).
3. Ôn lại câu sai + Lịch sử thi thử: test Free bị chặn HOÀN TOÀN (kể cả gọi thẳng API bằng
   Postman/curl, không chỉ ẩn UI) — xác nhận backend trả 403 đúng error code.
4. Cấp Premium thủ công: test cả 2 case — user đang Free/hết hạn (kích hoạt mới, reset 3 thẻ)
   và user đang Premium còn hạn (cộng dồn, giữ nguyên thẻ đã dùng). Nếu có thể, thử race condition
   thực tế (2 tab cùng cấp Premium 1 user gần như đồng thời).
5. Streak freeze: cần test kịch bản thực tế — Premium bỏ lỡ ĐÚNG 1 ngày (còn thẻ) → streak sống;
   bỏ lỡ 2 ngày trở lên → streak đứt dù còn thẻ; Free luôn 0 thẻ.
6. Cron cảnh báo Premium sắp hết hạn (3:05 AM hằng ngày) — khó test qua UI trực tiếp, có thể cần
   gọi thẳng premiumService.notifyExpiringPremiumUsers() qua script hoặc chỉnh giờ hệ thống test.
7. Badge "⭐ Premium" ở ProfilePage + thông tin hiển thị ở trang Tiến độ (hạn Premium, thẻ còn lại)
   — kiểm tra hiển thị đúng cho cả Free và Premium.
8. Đã sửa cấu trúc openapi.yaml (không phải nội dung nghiệp vụ) — không cần S5 test riêng phần
   này, chỉ liên quan tài liệu.

👉 Yêu cầu: Đưa ra danh sách kiểm thử thực tế và hướng dẫn test.
