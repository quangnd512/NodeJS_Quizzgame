[TỪ S2-THOCODE]

✅ ĐÃ HOÀN THÀNH: Admin User Management + Dashboard (008)
🌿 BRANCH: feature/admin-user-management

📂 FILE ĐÃ TẠO/SỬA:
- backend/prisma/schema.prisma (sửa — thêm isBlocked, role vào model User)
- backend/prisma/migrations/20260705120000_add_user_isblocked_role/ (mới)
- backend/src/middleware/auth.middleware.ts (sửa — check isBlocked → 403, Redis online tracking)
- backend/src/services/auth/auth.errors.ts (sửa — thêm UserBlockedError)
- backend/src/services/admin-users/admin-users.service.ts (mới)
- backend/src/services/admin-users/admin-users.errors.ts (mới)
- backend/src/services/admin-users/admin-users.types.ts (mới)
- backend/src/routes/admin-users.route.ts (mới)
- backend/src/app.ts (sửa — đăng ký router + error codes)
- frontend/src/lib/api.ts (sửa — 7 API functions mới)
- frontend/src/App.tsx (sửa — 2 tab Dashboard + Users + 2 component mới)
- frontend/src/App.css (sửa — CSS cho Dashboard + Users)

🔧 CÔNG VIỆC ĐÃ LÀM (10 TASK):
- TASK 1: DB schema + migration isBlocked + role → ✅
- TASK 2: verifyAppToken check isBlocked (403) + Redis online:{userId} EX 300 → ✅
- TASK 3: GET /api/admin/dashboard — 6 chỉ số tổng quan → ✅
- TASK 4: GET /api/admin/users + GET /api/admin/users/:id → ✅
- TASK 5: PATCH /api/admin/users/:id/block → ✅
- TASK 6: POST /api/admin/users/:id/reset-password (Firebase reset link) → ✅
- TASK 7: PATCH /api/admin/users/:id/role → ✅
- TASK 8: DELETE /api/admin/users/:id (Firebase + DB) → ✅
- TASK 9: Frontend Tab "Dashboard" — 6 thẻ số liệu → ✅
- TASK 10: Frontend Tab "Users" — danh sách + modal + tất cả actions → ✅

🌐 API MỚI:
- GET /api/admin/dashboard
- GET /api/admin/users?search=&role=&isBlocked=&page=&limit=
- GET /api/admin/users/:id
- PATCH /api/admin/users/:id/block — body: { isBlocked: boolean }
- POST /api/admin/users/:id/reset-password
- PATCH /api/admin/users/:id/role — body: { role: "STUDENT"|"ADMIN" }
- DELETE /api/admin/users/:id

🗄️ DATABASE:
- Bảng users: thêm cột isBlocked (Boolean default false), role (String default "STUDENT")
- Migration: 20260705120000_add_user_isblocked_role

🧪 TEST HIỆN TẠI:
- Backend unit tests: 18/18 PASS
- Build + lint: PASS

⚠️ LƯU Ý KHI REVIEW:
- ExamSession không có Prisma relation tới ExamPaper — service dùng batch query riêng (tránh N+1)
- Redis online tracking là fire-and-forget (không crash nếu Redis down)
- Reset password dùng Firebase generatePasswordResetLink, trả link qua window.prompt
- Xoá user: Firebase trước → DB sau; nếu DB lỗi thì ghi log và vẫn trả success
- eslint-disable-next-line cho 2 useEffect mới (pattern nhất quán với codebase hiện có)

API contract chi tiết: docs/api/drafts/admin-user-management.yaml

👉 Yêu cầu: Review toàn bộ code trên branch theo 7 tiêu chí + API contract compliance,
sửa lỗi, clear code, viết chú thích tiếng Việt, bổ sung test case còn thiếu.
