[TỪ S2-THOCODE]

🎯 TÍNH NĂNG VỪA LÀM: Anti-Cheat Security Fixes
🌿 BRANCH: feature/anti-cheat-fixes
📦 COMMIT: d1f32a0

📝 TÓM TẮT THAY ĐỔI:
Sửa 4 lỗ hổng bảo mật cho phép gian lận trong module thi thử và luyện tập.
Không thay đổi DB schema, không thêm endpoint mới, không migration cần chạy.

📁 FILES ĐÃ SỬA:
1. backend/src/services/exam/exam.types.ts
   - Thêm hằng số EXAM_MIN_SUBMIT_RATIO = 0.3
   - ExamWrongAnswerItem.correctAnswer: JsonValue | null (cho câu bỏ trắng)

2. backend/src/services/exam/exam.errors.ts
   - Thêm ExamSubmitTooEarlyError (code: EXAM_SUBMIT_TOO_EARLY, HTTP 400)
   - Thêm ExamSessionAlreadyActiveError (code: EXAM_SESSION_ALREADY_ACTIVE, HTTP 409)

3. backend/src/app.ts
   - Đăng ký 2 error code mới vào ERROR_CODE_TO_HTTP_STATUS

4. backend/src/services/exam/exam.service.ts
   - Import thêm: ExamSessionAlreadyActiveError, ExamSubmitTooEarlyError, EXAM_MIN_SUBMIT_RATIO
   - startExam(): Thêm helper isSentinelUnanswered(); check IN_PROGRESS session trước khi tạo mới
   - submitExam(): Check elapsed < 30% duration → throw ExamSubmitTooEarlyError
   - getExamResult(): Detect sentinel {} → set correctAnswer = null

5. backend/src/services/practice/practice.service.ts
   - checkRateLimit(): Đổi catch block → fail-closed (Redis lỗi → throw, không bỏ qua)
   - completeSession(): Thêm elapsed check bên trong $transaction với 60s grace

6. frontend/src/App.tsx
   - handleStart(): Thêm EXAM_SESSION_ALREADY_ACTIVE → hiển thị hubError
   - handleSubmit(): Thêm EXAM_SUBMIT_TOO_EARLY → hiển thị sessionError (không dùng globalError)
   - ExamSessionScreen: Thêm prop submitError + onClearSubmitError, render lỗi trước nút Nộp bài
   - Wrong answer map: correctAnswer === null → "Bạn chưa trả lời câu này" thay vì hiện đáp án

📋 DEFINITION OF DONE (để review):
□ POST /api/exam/submit khi chưa đủ 30% thời gian → trả 400 với message rõ ràng
□ POST /api/exam/submit sau 30% thời gian → thành công bình thường
□ POST /api/exam/start khi đang có phiên IN_PROGRESS cùng môn → trả 409
□ POST /api/exam/start sau khi phiên cũ kết thúc → thành công bình thường
□ GET /api/exam/:id/result: câu bỏ trắng → correctAnswer = null
□ GET /api/exam/:id/result: câu có trả lời → correctAnswer bình thường
□ POST /api/practice/start khi Redis down → trả lỗi, không cho tạo phiên
□ POST /api/practice/complete sau khi session hết giờ + 60s grace → trả lỗi
□ POST /api/practice/complete trong giờ → thành công bình thường
□ Frontend hiển thị đúng tất cả trường hợp trên
□ Tất cả unit test hiện có vẫn PASS (44 tests) ✅ ĐÃ KIỂM TRA
□ Build BE + FE không lỗi TypeScript / lint ✅ ĐÃ KIỂM TRA

⚠️ LƯU Ý KHI REVIEW:
- Sentinel {} là Prisma.JsonValue dạng object rỗng — detect trong isSentinelUnanswered()
- Fail-closed ở checkRateLimit: khi Redis down → user không thể bắt đầu phiên (UX chấp nhận được vì Redis hiếm khi down, bảo mật quan trọng hơn)
- Timeout check trong completeSession nằm BÊN TRONG $transaction để tránh race condition
- Frontend error "too early" dùng sessionError state riêng (không dùng global toast) để không che nội dung thi
