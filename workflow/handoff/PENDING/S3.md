[TỪ S2-THOCODE]

## ✅ ĐÃ HOÀN THÀNH (code): Đợt 1a — Nền móng App Di Động (Mobile Foundation)

🌿 BRANCH: feature/mobile-foundation (local, chưa push — xem lưu ý vận hành cuối file)

📂 FILE ĐÃ TẠO/SỬA (100% trong `mobile/` — 0 thay đổi ở `frontend/`, `backend/` — đã verify bằng `git diff 1d5bf8b..HEAD -- frontend backend` rỗng)

Thư mục mới `mobile/` (Expo SDK 57 + TypeScript), ~1800 dòng code trong 33 file `src/`, cấu trúc chính:
- `src/config/env.ts`, `src/api/{client,auth,users,admin,hello}.ts` — kết nối backend
- `src/auth/{firebase,googleSignIn,appleSignIn,AuthContext}.tsx|ts` — luồng đăng nhập học sinh
- `src/admin/{AdminAuthContext,AdminLoginScreen,AdminHomeScreen}.tsx` — luồng admin tách biệt
- `src/theme/{colors,ThemeContext}.ts|tsx`, `src/network/NetworkBanner.tsx`
- `src/navigation/*`, `src/screens/*`, `src/storage/secureStore.ts`, `src/constants/subjects.ts`
- `app.json`, `README.md`, `.env.example`, `eslint.config.js`

🔧 CÔNG VIỆC ĐÃ LÀM (13 commit riêng biệt trên `feature/mobile-foundation`, 1 commit/task):
- TASK 1-12: ✅ Done tất cả (xem `git log feature/mobile-foundation`)
- TASK 6 (giữ phiên): tích hợp sẵn vào TASK 2+4 (listener 401 trong `api/client.ts` + boot-restore trong `AuthContext`), không có commit riêng
- TASK 10 (dark mode picker UI): nằm trong `ProfileScreen` (commit TASK 8), phần lõi state/persistence ở commit TASK 10 riêng

🌐 API: không có API mới — chỉ tái sử dụng `POST /api/auth/login`, `GET /api/users/me`, `POST /api/users/subjects` theo đúng `docs/api/drafts/mobile-foundation.yaml`. Đã `curl` xác nhận backend đang chạy phản hồi đúng qua `/api/hello`.

🗄️ DATABASE: không có thay đổi.

🧪 TEST: **chưa viết unit test tự động.** Lý do: DoD của S1 không liệt kê yêu cầu test tự động (toàn bộ tiêu chí đều là hành vi cần verify thủ công trên thiết bị/giả lập), và đợt này chủ yếu là wiring UI/navigation/context — chưa có "service layer" nghiệp vụ như backend. Phần logic thuần gần nhất (`resolveApiBaseUrl` trong `config/env.ts`) có thể viết test được nhưng cần cài thêm `jest-expo` (chưa có trong repo) — **cố ý bỏ qua ở đợt này**, cờ lại cho S3 quyết định có bắt buộc bổ sung trước khi merge không.

✅ Build kiểm tra được (trong sandbox này):
- `npx tsc --noEmit`: PASS, sạch hoàn toàn (2 lần chạy độc lập)
- `eslint .` (eslint-config-expo): PASS, 0 lỗi/warning
- `npm install` toàn bộ dependency: thành công

⚠️ LƯU Ý QUAN TRỌNG CHO SESSION 3 (không thể tự kiểm chứng trong sandbox này, cần verify thủ công):
1. **Chưa chạy được app thật** — sandbox này không có giả lập iOS/Android/thiết bị thật, nên toàn bộ hành vi runtime trong DoD (đăng nhập Google/Apple, chuyển tab, dark mode, network banner, giữ phiên 7 ngày...) mới chỉ được viết đúng theo tài liệu chính thức + compile sạch, CHƯA được chạy thử thực tế.
2. `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` đang để trống trong `.env.example` — cần Firebase/Google Cloud Console thật để điền, nút Google chưa test được đầu-cuối.
3. Google/Apple Sign-In là native module → cần `npx expo prebuild` + dev build (không chạy trong Expo Go thường) — đã ghi rõ trong README mục 4.
4. Apple Sign-In cần tài khoản Apple Developer Program (99 USD/năm) để test thật trên iOS — đúng như S1 đã cảnh báo trước.
5. `bundleIdentifier`/`package` tạm đặt `com.quizzgame.mobile` — cần xác nhận với chủ dự án trước khi phát hành thật.

⚠️ LƯU Ý VỀ VẬN HÀNH (kế thừa từ S1): branch `feature/mobile-foundation` hiện chỉ có ở LOCAL, chưa push lên GitHub được (lỗi mạng sandbox lúc S1 chạy). Trong lúc S2 làm việc, git cũng vài lần bị treo do `.git/index.lock` (có thể do nhiều session Claude chạy song song trên chung working directory) — đã tự xử lý bằng cách xoá lock file khi không có tiến trình git nào thực sự đang giữ nó. Nếu S3 gặp tình huống tương tự, kiểm tra `.git/*.lock` trước khi thử lại.

👉 Yêu cầu: Review toàn bộ code trên branch theo 7 tiêu chí + API contract compliance,
sửa lỗi, clear code, viết chú thích tiếng Việt, bổ sung test case còn thiếu.
