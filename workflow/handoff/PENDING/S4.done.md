[TỪ S3-SOATLOI]

🎯 TÍNH NĂNG VỪA REVIEW: Exam UX Improvements (Feature 012)
🌿 BRANCH: feature/exam-ux-improvements

[S3-SoatLoi] ✅ REVIEW + TEST XONG: Exam UX Improvements
🌿 BRANCH: feature/exam-ux-improvements

📋 KẾT QUẢ REVIEW (7+1 tiêu chí):
  Lỗi tìm thấy: 3 — đã sửa hết
  ① CSS thiếu — thêm .exam-resume-banner, .exam-resume-msg, .exam-resume-actions, .btn-sm
  ② API draft lệch — bổ sung field title vào exam-ux-improvements.yaml
  ③ Unit test thiếu — thêm 12 test cho getActiveSession + abandonSession

🧪 KẾT QUẢ TEST TỰ ĐỘNG:
  Unit test: 78/78 PASS (+12 test mới)
  TypeScript: PASS (BE + FE)
  Lint: PASS (BE + FE)

🔍 ĐỐI CHIẾU THIẾT KẾ S1: API contract khớp sau khi bổ sung title

📁 FILES ĐÃ THAY ĐỔI TRONG FEATURE NÀY:
Backend:
  - backend/prisma/schema.prisma (cập nhật comment status field)
  - backend/src/services/exam/exam.types.ts (thêm ABANDONED, ActiveExamSessionResponse)
  - backend/src/services/exam/exam.errors.ts (thêm ExamSessionAbandonedError)
  - backend/src/services/exam/exam.service.ts (thêm getActiveSession, abandonSession)
  - backend/src/routes/exam.route.ts (thêm 2 endpoint mới)
  - backend/src/app.ts (thêm EXAM_SESSION_ABANDONED → 409)
  - backend/src/services/exam/__tests__/exam-ux.test.ts (12 unit test mới)
Frontend:
  - frontend/src/lib/api.ts (thêm getActiveExamSession, abandonExam)
  - frontend/src/App.tsx (resume flow, localStorage draft, nút Thoát)
  - frontend/src/App.css (thêm CSS classes mới)
Docs:
  - docs/api/drafts/exam-ux-improvements.yaml
  - docs/TEST_CASES.md
  - docs/CODE_REVIEW_LOG.md

📝 TÓM TẮT TÍNH NĂNG (để viết tài liệu):
3 cải tiến trải nghiệm thi thử:

1. RESUME — Quay lại bài thi đang dở:
   - Khi mở lại ExamPage, app gọi GET /api/exam/active
   - Nếu có phiên dở còn giờ → banner "Bạn có bài thi X đang dở (còn Y phút), tiếp tục không?"
   - Chọn "Tiếp tục" → load lại đáp án từ localStorage, vào bài ngay
   - Chọn "Huỷ bài" → gọi POST /api/exam/:id/abandon, về chọn môn
   - Nếu phiên dở đã hết giờ → tự submit với đáp án đã lưu → hiện kết quả

2. NÚT THOÁT — Thoát bài có xác nhận:
   - Nút ✕ ở góc trên trái màn hình thi
   - Hiện dialog "Bạn có chắc muốn thoát? Bài thi sẽ bị huỷ."
   - Xác nhận → gọi POST /api/exam/:id/abandon → về hub

3. THI MÔN KHÁC SAU KHI THOÁT:
   - Sau khi abandon → status = ABANDONED
   - startExam() không còn bị chặn 409 cho bất kỳ môn nào

📋 API MỚI CẦN GHI VÀO DOCS:
- GET /api/exam/active → { session: { id, subject, title, durationMinutes, startedAt, remainingSeconds } | null }
- POST /api/exam/:id/abandon → { success: true } | 403 | 404 | 409

👉 Yêu cầu: Viết/cập nhật tài liệu đầy đủ cho tính năng này.
Cụ thể cần làm:
1. Cập nhật docs/FEATURE_LOG.md — thêm entry cho Feature 012
2. Cập nhật docs/GLOSSARY.md — thêm các thuật ngữ mới nếu cần
3. Cập nhật docs/LESSONS_LEARNED.md — ghi lại bài học từ vòng này
4. Cập nhật docs/PROJECT_OVERVIEW.md nếu cần bổ sung thông tin tính năng mới
