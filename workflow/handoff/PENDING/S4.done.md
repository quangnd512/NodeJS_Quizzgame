[TỪ S8-GIAMSAT]

↩️ YÊU CẦU LÀM LẠI (nhỏ): Notifications — Thông báo hệ thống (Feature 013 / TASKS.md ID 009)
🌿 BRANCH: feature/notifications

📋 BỐI CẢNH: S2 đã sửa xong lỗi thiếu logic điều hướng khi bấm thông báo (commit 293b40d) —
S8 đã tự kiểm chứng: đọc diff, chạy tsc --noEmit (FE+BE), npm test BE (89/89 PASS), npm run lint FE
(PASS) — đều khớp báo cáo của S2, code đúng theo DoD gốc của S1. Phần code coi như ĐẠT.

⚠️ VẤN ĐỀ CÒN LẠI (thuần tài liệu — không cần S2/S3 động vào code nữa):

1. docs/CHANGELOG.md dòng 24 (mục [Unreleased] — Notifications, phần Added):
   Hiện ghi: "**NotificationToast** — toast pop-up 4 giây khi có thông báo mới"
   → Cần sửa thành 7 giây (đã tăng theo yêu cầu người dùng từ vòng test S5, xem
   docs/FEATURE_LOG.md dòng ~6516 và LESSONS_LEARNED "Vòng 13" đã ghi đúng rồi, chỉ CHANGELOG.md
   là còn sai).
   → Nhân tiện: dòng "NotificationPanel (drawer slide-in) với chức năng đánh dấu đọc từng/tất cả
   thông báo" (dòng 23) nên bổ sung thêm ý "và điều hướng sang màn hình tương ứng theo targetScreen
   (progress/leaderboard/exam)" để phản ánh đúng tính năng mới nhất.

2. docs/guides/user-guide.md (mục "Thông báo hệ thống (Feature 013)", quanh dòng 1199-1227):
   a) Dòng ~1226: "...một popup nhỏ sẽ xuất hiện ở phía trên màn hình trong khoảng 4 giây..."
      → sửa thành 7 giây.
   b) Mục "### Đánh dấu đã đọc" (dòng ~1219-1222) hiện chỉ mô tả:
      "Bấm vào từng thông báo → đánh dấu đã đọc (chấm xanh biến mất)"
      → Đây là mô tả THIẾU: thực tế bấm vào thông báo còn điều hướng sang trang tương ứng (trừ
      loại "báo cáo được xử lý"). Đề nghị sửa/thêm mục mới, ví dụ:

      ### Bấm vào thông báo
      - 🔥 Streak milestone → đánh dấu đã đọc + chuyển sang Trang Tiến độ học tập
      - 🏆 Lên hạng / 📉 Xuống hạng → đánh dấu đã đọc + chuyển sang Trang Bảng xếp hạng
      - 📝 Đề thi mới → đánh dấu đã đọc + chuyển sang Trang Thi thử
      - ✅ Báo cáo được xử lý → chỉ đánh dấu đã đọc, KHÔNG chuyển trang (ở lại màn hình hiện tại)
      - Bấm "Đánh dấu tất cả đã đọc" → toàn bộ thông báo đều đọc (không điều hướng)

📌 KHÔNG cần đụng vào docs/FEATURE_LOG.md — mục Feature 013 ở đó đã đúng (7 giây, đã có phần
"Ghi chú kỹ thuật" nói rõ lịch sử sửa đổi). Cũng không cần đụng LESSONS_LEARNED.md.

👉 Khi sửa xong:
- Ghi kết quả (2 file đã sửa, tóm tắt thay đổi) vào workflow/handoff/PENDING/S8.md
- Thông báo người dùng để chuyển lại cho S8 xác nhận ĐẠT quality gate lần cuối (đây là bước
  cuối cùng còn thiếu trước khi cho phép S7 push & merge).
