[TỪ S3-SOATLOI]

✅ REVIEW + TEST XONG: Admin User Management + Dashboard (008)
🌿 BRANCH: feature/admin-user-management

📋 KẾT QUẢ REVIEW (7+1 tiêu chí):
- Lỗi tìm thấy: 2
  1. Thiếu index trên isBlocked + role → đã thêm @@index + sync DB + cập nhật migration SQL
  2. totalPages=0 khi không có data → đã fix Math.max(1, Math.ceil(total/limit))

🧪 KẾT QUẢ TEST:
- Unit test: 44/44 PASS (18 cũ + 26 mới cho adminUsersService)
- Backend TS: PASS | Frontend TS: PASS | Lint: PASS
- API contract: Tất cả 7 endpoint khớp đúng YAML draft

📁 FILES ĐÃ THAY ĐỔI (toàn bộ tính năng):
Backend:
  - backend/prisma/schema.prisma (isBlocked, role, 2 index mới)
  - backend/prisma/migrations/20260705120000_add_user_isblocked_role/
  - backend/src/middleware/auth.middleware.ts (check isBlocked → 403, Redis online tracking)
  - backend/src/services/auth/auth.errors.ts (UserBlockedError)
  - backend/src/services/admin-users/admin-users.service.ts
  - backend/src/services/admin-users/admin-users.errors.ts
  - backend/src/services/admin-users/admin-users.types.ts
  - backend/src/services/admin-users/__tests__/admin-users.service.test.ts
  - backend/src/routes/admin-users.route.ts
  - backend/src/app.ts
Frontend:
  - frontend/src/lib/api.ts (7 API functions mới)
  - frontend/src/App.tsx (2 tab Dashboard + Users, 2 component mới)
  - frontend/src/App.css (CSS cho Dashboard + Users)
Docs:
  - docs/api/drafts/admin-user-management.yaml
  - docs/TEST_CASES.md (30 test cases mới)
  - docs/CODE_REVIEW_LOG.md

🌐 API MỚI:
- GET /api/admin/dashboard — 6 chỉ số tổng quan
- GET /api/admin/users — list với search/filter/phân trang
- GET /api/admin/users/:id — chi tiết + stats + 5 kỳ thi gần nhất
- PATCH /api/admin/users/:id/block — khoá/mở khoá
- POST /api/admin/users/:id/reset-password — Firebase reset link
- PATCH /api/admin/users/:id/role — đổi quyền STUDENT/ADMIN
- DELETE /api/admin/users/:id — xoá Firebase + DB

🗄️ DATABASE:
- Bảng users: thêm isBlocked (Boolean default false), role (String default "STUDENT")
- Index mới: users_isBlocked_idx, users_role_idx

👉 Yêu cầu: Viết/cập nhật tài liệu đầy đủ cho tính năng này:
  - FEATURE_LOG.md (mô tả kỹ thuật chi tiết)
  - CHANGELOG.md (entry mới)
  - docs/guides/admin-guide.md (hướng dẫn admin dùng 2 tab mới)
  - PROJECT_OVERVIEW.md nếu cần cập nhật mô tả module
