[TỪ S4-GHICHEP]

✅ TÀI LIỆU XONG: Đợt 1a — Nền móng App Di Động (Mobile Foundation)
🌿 BRANCH: feature/mobile-foundation

📄 TÀI LIỆU ĐÃ CẬP NHẬT:
- **FEATURE_LOG.md**: Thêm Section 17 (Mobile Foundation) đầy đủ — tổng quan, data model (không đổi), API reference (4 endpoint tái sử dụng), 5 sơ đồ luồng chạy ASCII (boot/RootNavigator, Google/Apple login, nonce Apple, auto-logout 401, admin login), file structure, 6 mục ghi chú kỹ thuật, cách test thủ công, rủi ro/TODO.
- **openapi.yaml**: có — v1.7.1 → v1.8.0, bổ sung chính thức `POST /api/auth/login` + `GET /api/users/me` (2 endpoint cốt lõi trước đây bị bỏ sót từ trước Feature 001, chưa từng vào file này) kèm ví dụ request/response thật, mã lỗi thật; validate lại bằng pyyaml — parse sạch, 52 paths.
- **docs/api/drafts/mobile-foundation.yaml**: ghi chú đã merge 2 endpoint trên vào openapi.yaml chính.
- **admin-guide.md**: có — Section 17 mới (không có màn hình admin thật trên mobile đợt này; cơ chế xác thực admin bằng "ping"; SecureStore vs sessionStorage; đồng bộ SUBJECT_CATALOG 3 nơi; 4 mục xử lý sự cố).
- **user-guide.md**: có — mục mới "Ứng dụng Di động (Mobile App) — Đợt 1a" (đăng nhập lần đầu, 5 tab, tab Hồ sơ, giữ đăng nhập 7 ngày, banner mất mạng, đăng nhập Admin, FAQ).
- **CHANGELOG.md**: có — entry `[Unreleased] — Nền móng App Di Động` mới ở đầu file (Added/Fixed/Ghi chú).
- **troubleshooting.md**: có — 5 mục mới #32-36 (native module cần dev build, sai API URL theo môi trường, Apple Developer Program, đồng bộ subjects giữa web/mobile, kẹt màn Splash).
- **README.md (gốc)**: không áp dụng — repo không có README gốc; `mobile/README.md` đã được S2 viết đầy đủ + S3 xác nhận đúng, không cần sửa thêm.
- **GLOSSARY.md**: không đụng đến — thuộc trách nhiệm của S6 (Người Giảng Giải), không phải S4.

📋 BỐI CẢNH QUAN TRỌNG CHO S5 (kế thừa từ S3):
- Đây là feature THUẦN wiring UI/native-SDK — KHÔNG có test tự động (quyết định có cân nhắc,
  xem FEATURE_LOG.md Section 17 mục kỹ thuật #5 + CODE_REVIEW_LOG.md). Toàn bộ 17 dòng DoD gốc
  của S1 được khung theo hướng xác minh THỦ CÔNG trên thiết bị/giả lập thật — đây chính là lý
  do tồn tại của vòng test này, không phải bước hình thức.
- `docs/TEST_CASES.md` mục "Test Cases: Đợt 1a — Nền móng App Di Động" đã có sẵn 18 kịch bản
  test thủ công (9 happy path, 6 edge case, 3 lỗi) — đây là checklist chính để chạy.
- Cần môi trường thật: Google/Apple Sign-In là native module, KHÔNG chạy được trong Expo Go
  thường — phải build "dev build" (`npx expo prebuild` + `npx expo run:android`/`run:ios`,
  xem `mobile/README.md` mục 4c) trước khi test được kịch bản #1, #2, #10-13.
- Apple Sign-In (kịch bản #2) cần tài khoản Apple Developer Program để test thật trên iOS —
  nếu chưa có, có thể SKIP có ghi chú rõ lý do (giới hạn đã biết trước, không phải bug).
- Nhớ điền `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` trong `mobile/.env` trước khi test đăng nhập
  Google (xem `mobile/README.md` mục 4a).

👉 Yêu cầu: Đưa ra danh sách kiểm thử thực tế và hướng dẫn test theo 18 kịch bản đã có trong
docs/TEST_CASES.md (mục "Đợt 1a — Nền móng App Di Động").
