[TỪ S1-KIENTRUCSU]

🎯 TÍNH NĂNG CẦN LÀM: Quản lý câu hỏi (gộp "Học sinh đóng góp câu hỏi" + "Thiết kế lại luồng xử lý báo cáo câu hỏi")
🌿 BRANCH: feature/question-management-hub

📝 TÓM TẮT YÊU CẦU NGƯỜI DÙNG:
Người dùng muốn 1 khu "Quản lý câu hỏi" duy nhất trong trang Admin, gồm 2 tab con:
- "Bài học sinh gửi": học sinh gửi câu hỏi trắc nghiệm mới (nội dung + 4 đáp án + môn/chương),
  admin duyệt (vào ngân hàng câu hỏi thi thử + học sinh nhận 30 điểm) hoặc từ chối (kèm lý do).
  Mỗi lần câu được thêm vào 1 đề thi thật, học sinh nhận thêm 5 điểm (tối đa 100 điểm/câu).
  Học sinh xem được danh sách câu đã gửi + trạng thái, sửa/xoá được khi còn chờ duyệt.
- "Báo cáo lỗi": làm lại tính năng báo cáo câu hỏi sai đã có — hiện tại chỉ hiện mã UUID thô,
  giờ phải hiện đầy đủ nội dung câu hỏi. Bỏ trạng thái "Đã xem" (chỉ còn Chờ xử lý → Đã sửa/
  Đã bỏ qua). Admin sửa được nội dung câu hỏi ngay tại trang xử lý, sửa xong tự đóng báo cáo.
  Nếu câu bị nhiều học sinh báo cáo, sửa 1 lần đóng hết. Nếu câu bị ẩn tự động do báo cáo
  nhiều thì sửa xong tự hiện lại. Có bộ lọc theo môn/lý do, badge số lượng chờ xử lý trên tab,
  cảnh báo trùng lặp khi duyệt câu học sinh gửi, chống spam (giới hạn 5 câu/ngày/học sinh).

🔧 CHI TIẾT KỸ THUẬT:

Files ảnh hưởng chính:
 - backend/prisma/schema.prisma (2 bảng mới + enum NotificationType +3 giá trị)
 - backend/src/services/practice/practice.service.ts (report: JOIN Question, filter, resolve gộp, batch-close)
 - backend/src/services/practice/practice.types.ts (REPORT_STATUSES giữ nguyên cho dữ liệu cũ)
 - backend/src/routes/admin.route.ts (sửa reportStatusSchema chỉ nhận FIXED/DISMISSED, thêm route resolve gộp + routes submissions admin)
 - backend/src/services/exam/* (submission service mới, usage points trigger trong addFromBank)
 - backend/src/services/notification/notification.types.ts (thêm 3 NotificationType)
 - backend/src/services/points/points.service.ts (dùng addPoints có sẵn, reason mới: SUBMISSION_APPROVED/SUBMISSION_USED)
 - frontend/src/App.tsx (tab admin 'reports' → tab gộp "Câu hỏi" với 2 tab con; trang học sinh gửi câu hỏi)
 - frontend/src/lib/api.ts (API client mới)

DB schema mới (xem chi tiết đầy đủ field trong docs/api/drafts/quan-ly-cau-hoi.yaml):
 - student_question_submissions (userId, subject, chapter, questionText, options, correctOptionIndex,
   status PENDING/APPROVED/REJECTED, adminNote, questionBankId, usageCount, usagePointsEarned, timestamps)
   Index: @@index([userId, status, createdAt]), @@index([questionBankId])
 - question_edit_history (id, questionId, reportId?, beforeData Json, createdAt) — lưu bản gốc câu hỏi
   trước khi admin sửa từ trang xử lý báo cáo
 - enum NotificationType += SUBMISSION_APPROVED, SUBMISSION_REJECTED, SUBMISSION_USED

📄 API DRAFT ĐẦY ĐỦ: docs/api/drafts/quan-ly-cau-hoi.yaml (đọc kỹ trước khi code — có full request/response/side_effects)

📋 DANH SÁCH TASK:
TASK 1: DB — Migration: bảng student_question_submissions + question_edit_history + enum NotificationType (+3 giá trị) — Phụ thuộc: không
TASK 2: Backend — SubmissionService (học sinh): tạo/sửa/xoá (chỉ khi PENDING)/xem, validate đầy đủ + rate limit 5 câu/ngày/user (đếm theo createdAt trong ngày) — Phụ thuộc: TASK 1
TASK 3: Backend — SubmissionService (admin): danh sách chờ duyệt (kèm cờ cảnh báo trùng lặp — so khớp text đơn giản, chuẩn hoá bỏ dấu+viết thường, so với QuestionBank active cùng môn, chỉ cảnh báo không chặn), duyệt (tạo QuestionBank entry type=MCQ_4 isActive=true + addPoints 30 reason SUBMISSION_APPROVED + notify), từ chối (note bắt buộc, notify SUBMISSION_REJECTED) — Phụ thuộc: TASK 2
TASK 4: Backend — Routes submissions (student: POST/GET/PUT/DELETE /api/submissions[/:id]; admin: GET /api/admin/submissions, POST .../approve, POST .../reject) — Phụ thuộc: TASK 3
TASK 5: Backend — Usage points trigger: trong hàm thêm câu từ bank vào đề thi (examService addFromBank hoặc tương đương) — sau khi insert ExamQuestion, kiểm tra questionBankId có liên kết StudentQuestionSubmission không, nếu có và usagePointsEarned<100 thì addPoints min(5,100-current) reason SUBMISSION_USED + update usageCount/usagePointsEarned + notify — Phụ thuộc: TASK 3
TASK 6: Backend — Report redesign (không phụ thuộc phần trên): sửa GET /api/admin/questions/reports JOIN đầy đủ nội dung Question (question, options, correctAnswer, explanation, subject, chapter, difficulty, isActive), thêm filter subject/reason, sửa reportStatusSchema (dùng ở endpoint cũ nếu còn giữ) hoặc bỏ hẳn PATCH cũ để thay bằng TASK 7, sửa getReportsSummary trả thêm pendingQuestions (COUNT DISTINCT questionId WHERE status=PENDING) bên cạnh pendingReports (COUNT status=PENDING) — Phụ thuộc: không
TASK 7: Backend — Endpoint gộp PATCH /api/admin/questions/reports/:id/resolve {status, questionUpdate?}: nếu có questionUpdate → lưu snapshot cũ (toàn bộ field Question hiện tại) vào question_edit_history TRƯỚC khi update, rồi update Question; update report hiện tại → status; sau đó tìm mọi QuestionReport khác cùng questionId đang PENDING → cùng set status, MỖI report gọi lại fireReportResolvedNotification riêng (giữ nguyên hàm có sẵn, không đổi logic notify từng user); nếu status=FIXED và Question.isActive trước đó = false → set isActive=true trong cùng transaction; validate status chỉ nhận FIXED|DISMISSED (400 nếu gửi giá trị khác kể cả REVIEWED) — Phụ thuộc: TASK 6
TASK 8: Frontend — Form gửi câu hỏi (học sinh): chọn môn, nhập chương (optional), nội dung, 4 ô đáp án, radio chọn đáp án đúng, validate không rỗng — Phụ thuộc: TASK 4
TASK 9: Frontend — Danh sách câu đã gửi (học sinh): badge trạng thái (🟡 Chờ/✅ Duyệt/❌ Từ chối), xem lý do từ chối, nút Sửa/Xoá hiện khi còn PENDING — Phụ thuộc: TASK 8
TASK 10: Frontend — Cải tổ Admin: đổi tab 'reports' hiện có thành tab "Câu hỏi" với 2 tab con ("Bài học sinh gửi" / "Báo cáo lỗi"), thêm badge số đỏ trên tab cha (tổng pending 2 phần) — Phụ thuộc: TASK 4, TASK 6
TASK 11: Frontend — Admin UI "Bài học sinh gửi": danh sách + hiện cảnh báo trùng lặp nếu có + modal xem chi tiết + nút Duyệt/Từ chối (nhập lý do) — Phụ thuộc: TASK 10
TASK 12: Frontend — Admin UI "Báo cáo lỗi" (thay AdminReportsPage cũ): hiện đầy đủ nội dung câu hỏi thay vì mã UUID, form sửa tại chỗ (pre-fill từ Question), nút "Lưu & Đánh dấu đã sửa" / "Bỏ qua", bộ lọc môn học + lý do báo cáo — Phụ thuộc: TASK 10

📋 DEFINITION OF DONE:
□ Học sinh gửi câu hỏi thành công (nội dung+4 đáp án+đáp án đúng+môn/chương); vượt 5 câu/ngày → bị chặn rõ ràng (400)
□ Học sinh sửa/xoá được câu khi còn PENDING; không sửa/xoá được khi đã APPROVED/REJECTED (409)
□ Admin thấy cảnh báo nếu câu gửi trùng/gần giống câu đã có trong kho cùng môn
□ Admin duyệt → câu vào ngân hàng câu hỏi thi thử + học sinh nhận 30 điểm + thông báo SUBMISSION_APPROVED
□ Admin từ chối kèm lý do → học sinh thấy lý do + nhận thông báo SUBMISSION_REJECTED
□ Câu từ ngân hàng được thêm vào đề thi → học sinh nhận +5 điểm + thông báo SUBMISSION_USED, tổng không vượt 100đ/câu
□ Trang xử lý báo cáo hiển thị đầy đủ nội dung câu hỏi (không chỉ mã UUID)
□ API không còn cho phép set trạng thái "Đã xem" (REVIEWED) qua endpoint mới — chỉ FIXED/DISMISSED
□ Admin sửa được nội dung câu hỏi ngay tại trang xử lý báo cáo; bản gốc trước khi sửa được lưu vào question_edit_history
□ Đánh dấu "Đã sửa" → report hiện tại + mọi report PENDING khác cùng câu đều chuyển FIXED, mỗi học sinh báo cáo nhận thông báo riêng
□ Câu bị tự động ẩn (≥5 báo cáo, AUTO_HIDE_REPORT_THRESHOLD) → sau khi đánh dấu Đã sửa, tự động hiện lại (isActive=true)
□ Trang báo cáo lọc được theo môn học và theo lý do báo cáo
□ Ô thống kê "Chờ xử lý" hiển thị 2 số rõ ràng (số báo cáo / số câu hỏi khác nhau), khớp với danh sách
□ Badge số lượng chờ xử lý hiển thị đúng trên tab Admin (gồm cả bài gửi mới + báo cáo lỗi)
□ POST /api/submissions validate đầy đủ (thiếu field/đáp án ngoài 0-3/vượt rate limit → 400)
□ Endpoint admin chỉ admin gọi được (sai/thiếu X-Admin-Secret → 401)
□ Build BE+FE không lỗi, test cũ (89/89) vẫn PASS, thêm test cho luồng mới

✅ YÊU CẦU:
- Checkout branch: feature/question-management-hub (đã tạo & push sẵn)
- Đọc kỹ docs/api/drafts/quan-ly-cau-hoi.yaml trước khi code (có full request/response/side_effects từng endpoint)
- Thực hiện lần lượt từng TASK ở trên (TASK 1-5 và TASK 6-7 độc lập nhau, có thể làm song song nếu muốn, nhưng TASK 8-12 phụ thuộc backend tương ứng)
- Khi xong, tổng kết công việc và báo Session 3 review

⚠️ LƯU Ý ĐẶC BIỆT:
- Đã XÁC MINH: hệ thống báo cáo (QuestionReport) chỉ áp dụng cho module Ôn tập (model `Question`),
  KHÔNG liên quan tới ExamQuestion/QuestionBank (module Thi thử). KHÔNG cần viết code đồng bộ
  nội dung câu hỏi cho lịch sử Ôn tập cũ — UserQuestionHistory không lưu snapshot content, nên
  sửa Question tự động phản ánh mọi nơi ngay lập tức. Đừng over-engineer phần này.
- usagePointsEarned tối đa 100/câu: cộng min(5, 100-current) mỗi lần dùng, không cộng âm.
- Tất cả points + notification dùng fire-and-forget, không block response admin (theo pattern
  đã dùng ở Feature 013 Notifications).
- reportId trong question_edit_history nullable vì có thể admin sửa câu hỏi từ nơi khác trong
  tương lai (không bắt buộc phải qua report) — hiện tại chỉ cần set khi sửa qua luồng resolve.
- Khi batch-resolve các report PENDING khác cùng questionId, đảm bảo dùng transaction để tránh
  race condition nếu 2 báo cáo được xử lý gần như đồng thời.
- Repo đã dùng chung 1 working directory (không phải git worktree riêng) cho các session —
  S7 từng gặp tranh chấp git khi 2 session chạy song song. Tránh chạy song song với session
  khác đang thao tác git trên cùng thư mục này.
