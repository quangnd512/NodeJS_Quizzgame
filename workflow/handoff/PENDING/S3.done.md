[TỪ S2-THOCODE]

🎯 TÍNH NĂNG VỪA LÀM: Exam UX Improvements (Feature 012)
🌿 BRANCH: feature/exam-ux-improvements
📦 COMMITS: ced8e6b → bfbd8d2 → 8da5823

📝 TÓM TẮT THAY ĐỔI:
Thêm 3 cải tiến trải nghiệm thi thử:
1. Resume — lỡ thoát app → mở lại → hỏi "Tiếp tục?" hoặc tự nộp nếu hết giờ
2. Nút Thoát — trong màn hình thi, hỏi xác nhận trước khi huỷ bài
3. Thi môn khác ngay — sau khi huỷ bài không còn bị chặn 409

📁 FILES ĐÃ SỬA:

1. backend/prisma/schema.prisma
   - Cập nhật comment field status: bổ sung "ABANDONED"

2. backend/src/services/exam/exam.types.ts
   - EXAM_SESSION_STATUSES: thêm 'ABANDONED'
   - Thêm interface ActiveExamSessionResponse (cho GET /api/exam/active)

3. backend/src/services/exam/exam.errors.ts
   - Thêm ExamSessionAbandonedError (code: EXAM_SESSION_ABANDONED, HTTP 409)

4. backend/src/app.ts
   - Thêm EXAM_SESSION_ABANDONED → 409 trong ERROR_CODE_TO_HTTP_STATUS

5. backend/src/services/exam/exam.service.ts
   - submitExam(): thêm check ABANDONED → throw ExamSessionAbandonedError
   - getExamResult(): thêm ABANDONED vào điều kiện throw ExamSessionNotCompletedError
   - Thêm getActiveSession(userId): trả phiên IN_PROGRESS + remainingSeconds
   - Thêm abandonSession(userId, sessionId): đổi status → ABANDONED

6. backend/src/routes/exam.route.ts
   - Thêm GET /api/exam/active
   - Thêm POST /api/exam/:id/abandon

7. frontend/src/lib/api.ts
   - Thêm interface ActiveExamSession
   - Thêm getActiveExamSession(token)
   - Thêm abandonExam(token, sessionId)

8. frontend/src/App.tsx
   - helpers: saveDraftAnswers, loadDraftAnswers, clearDraftAnswers (localStorage)
   - ExamPage: useEffect kiểm tra phiên dở khi mount, banner resume, logic handleResume/handleAbandonFromResume
   - handleStart: lưu StartExamResult vào localStorage (exam_session_data_{sessionId})
   - handleAnswerChange: gọi saveDraftAnswers sau mỗi lần chọn
   - handleSubmit: clearDraftAnswers sau khi nộp thành công
   - handleAbandonFromSession: gọi abandonExam rồi về hub
   - ExamSessionScreen: thêm prop onAbandon, state showExitConfirm
   - ExamSessionScreen: nút ✕ ở topbar + modal xác nhận "Huỷ bài thi / Ở lại"

📋 DEFINITION OF DONE (để review):
□ GET /api/exam/active → trả về session đang dở kèm remainingSeconds chính xác
□ GET /api/exam/active → trả null khi không có session nào IN_PROGRESS
□ POST /api/exam/:id/abandon → 200, session đổi thành ABANDONED
□ POST /api/exam/:id/abandon của session không thuộc user → 403
□ Sau khi ABANDON → POST /api/exam/start bất kỳ môn → không còn bị 409
□ Vào màn hình thi khi có session dở còn giờ → hiện dialog hỏi tiếp tục
□ Chọn "Tiếp tục": đồng hồ đúng thời gian còn lại, đáp án cũ được khôi phục
□ Chọn "Huỷ" từ dialog: session bị abandon, có thể thi môn khác ngay
□ Vào khi hết giờ: tự động nộp → hiện kết quả (hoặc thông báo hết hạn nếu quá grace)
□ Trong bài: nút "Thoát" → hộp xác nhận → xác nhận → abandon → ra ngoài
□ Không xác nhận thoát → ở lại làm tiếp bình thường
□ Mỗi lần chọn đáp án → tự lưu vào localStorage
□ Build BE + FE không lỗi ✅ ĐÃ KIỂM TRA
□ 66/66 unit test PASS ✅ ĐÃ KIỂM TRA

⚠️ LƯU Ý KHI REVIEW:
- CSS classes mới trong ExamPage hub chưa có trong App.css:
  exam-resume-banner, exam-resume-msg, exam-resume-actions
  → Cần thêm style hoặc dùng inline style cho các class này
- Resume chỉ hoạt động khi localStorage có exam_session_data_{sessionId}
  (được lưu lúc handleStart). Nếu user clear localStorage thì resume sẽ tự abandon phiên cũ.
  Đây là trade-off chủ ý — không thêm endpoint GET /api/exam/:id/questions riêng.
- abandonSession trả 409 khi session đã ABANDONED (idempotent-safe, không crash)
- remainingSeconds trong GET /api/exam/active có thể âm → frontend xử lý đúng (auto-submit)

👉 Yêu cầu: Review toàn bộ code trên branch theo 7 tiêu chí + API contract compliance,
sửa lỗi, clear code, viết chú thích tiếng Việt, bổ sung test case còn thiếu.
Đặc biệt kiểm tra CSS classes mới (exam-resume-banner...) chưa được định nghĩa.
