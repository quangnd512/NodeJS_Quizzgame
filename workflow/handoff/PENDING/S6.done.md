[TỪ S5-THUNGHIEM]

[S5-ThuNghiem] ✅ KIỂM THỬ XONG: Notifications — Thông báo hệ thống (Feature 013)
🌿 BRANCH: feature/notifications

📊 KẾT QUẢ:
- Tổng tests: 26 (12 Happy Path + 5 Edge Cases + 4 Error Cases + 5 Security)
- Pass: 26 ✅
- Fail: 0 ✅

🐛 BUGS ĐÃ TÌM & SỬA TRONG PHIÊN TEST (frontend/src/App.tsx, frontend/src/App.css):
1. Toast bị tắt sớm khi 2 thông báo đến gần nhau — setTimeout tắt toast không so sánh
   id, nên timeout của thông báo CŨ có thể tắt nhầm thông báo MỚI trước hạn. Đã sửa
   bằng cách so sánh current?.id === newest.id trong callback trước khi setToast(null).
2. Thời gian hiển thị toast quá ngắn (4 giây) — kéo dài lên 7 giây theo yêu cầu người
   dùng (dễ bắt kịp hơn, đặc biệt khi 2 thông báo đến gần nhau).

✨ CẢI THIỆN THÊM (theo yêu cầu người dùng, không phải bug):
- Poll ngay khi tab được focus lại (visibilitychange listener) — giảm cảm giác "phải
  reload mới thấy thông báo" khi người dùng rời tab (VD: chuyển sang xem terminal)
  rồi quay lại.
- Làm nổi bật riêng thông báo RANK_UP: nền vàng gold + hiệu ứng phát sáng nhấn nhá,
  khác biệt so với 4 loại thông báo còn lại (mỗi loại có viền màu accent riêng).

📋 GHI NHẬN NGOÀI PHẠM VI (đã lưu kế hoạch riêng cho S1 làm SAU, không thuộc Feature 013):
- Người dùng yêu cầu redesign luồng xử lý báo cáo câu hỏi trong Admin: bỏ trạng thái
  "Đã xem" (REVIEWED), hiện chi tiết câu hỏi (không chỉ ID) ngay tại trang xử lý báo
  cáo, cho sửa câu hỏi tại đó, và đồng bộ sửa ở mọi nơi câu hỏi được dùng.
  → Đã lưu chi tiết tại workflow/handoff/PENDING/S1_saved_plan_admin_report_workflow.md
  (kèm ghi chú rủi ro kiến trúc: hiện ExamQuestion là bản sao copy-on-add từ
  QuestionBank, không phải live-reference — cần S1 quyết định mô hình đồng bộ).

⚠️ SỰ CỐ HẠ TẦNG (không phải bug code, không ảnh hưởng kết quả cuối):
- Trong lúc test, backend bị gián đoạn nhiều lần (có gì đó liên tục "chạm" file
  backend/src khiến tsx watch restart lặp lại/crash) và Redis từng không chạy (gây
  429 rate-limit giả ở module Practice — fail-closed theo thiết kế). Đã khởi động lại
  backend + Redis, không phải lỗi của Feature 013.

📋 BỐI CẢNH TỪ S3 (review + test tự động trước đó — không lặp lại trong lần test này):
- 89/89 unit test PASS | Build PASS | 24 test case tích hợp trong docs/TEST_CASES.md
  (đã verify khớp toàn bộ trong lần test thủ công này, không phát hiện sai lệch)

👉 Yêu cầu: Giải thích tính năng Notifications cho người dùng (bell icon, toast,
panel, 5 loại thông báo, polling 30s), hỏi xem họ có thắc mắc gì không.
