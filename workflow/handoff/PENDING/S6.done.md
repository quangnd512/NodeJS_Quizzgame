[TỪ S5-THUNGHIEM]

[S5-ThuNghiem] ✅ KIỂM THỬ XONG: Quản lý câu hỏi — Học sinh đóng góp câu hỏi + Report Redesign (Feature 014)
🌿 BRANCH: feature/question-management-hub

📊 KẾT QUẢ:
- Tổng tests: 19 (9 Happy Path + 8 Edge Cases + 2 Error Cases) + 3 Security Check
- Pass: 22 ✅
- Fail: 0 ✅

🐛 CẢI TIẾN/SỬA ĐỔI PHÁT SINH TRONG PHIÊN TEST (đều đã qua `tsc` sạch (BE+FE) +
137/137 unit test PASS, cần S6 giải thích thêm cho người dùng vì đây là thay đổi
so với thiết kế gốc của S1):

1. **Bỏ trường "Chương" khỏi form gửi câu hỏi** + **thêm chọn dạng câu hỏi**
   (MCQ_4 / TRUE_FALSE_4 / FILL_BLANK — giống hệt module Thi thử, dùng chung
   `validateQuestionShape()`). Migration DB: đổi `correctOptionIndex` (Int) →
   `correctAnswer` (Json, linh hoạt theo dạng), `options` giờ nullable (null với
   FILL_BLANK). Migration: `20260713100000_submission_question_types`.

2. **Cho phép học sinh báo cáo lại 1 câu đã từng báo cáo**, NẾU báo cáo cũ đã
   được xử lý xong (FIXED/DISMISSED) — trước đây bị chặn vĩnh viễn (1 báo cáo/
   câu/học sinh). Có hộp thoại xác nhận "Bạn đã báo cáo câu hỏi này rồi (đã được
   xử lý). Bạn có muốn báo cáo lại không?". Migration: đổi unique constraint
   tuyệt đối thành PARTIAL UNIQUE INDEX (chỉ chặn khi còn PENDING). Lỗi mới:
   `REPORT_RESUBMIT_CONFIRM_REQUIRED` (409). Migration:
   `20260714000000_report_resubmit_after_resolved`.

3. **Lỗi vượt rate-limit 5 câu/ngày hiển thị dạng modal box** thay vì chữ đỏ
   inline (các lỗi validate khác vẫn giữ nguyên inline).

4. **Cảnh báo trùng lặp hiện kèm nội dung + đáp án đầy đủ** của câu trùng trong
   Ngân hàng câu hỏi (trước đây chỉ có % tương đồng, không biết trùng với câu nào).

5. **Lọc liên động 3 dropdown** (trạng thái/môn/lý do) trong trang Báo cáo lỗi —
   mỗi dropdown chỉ hiện giá trị thực sự có dữ liệu khớp với 2 điều kiện lọc còn
   lại. Endpoint mới: `GET /api/admin/questions/reports/facets`.

📋 BỐI CẢNH TỪ S3 (review + test tự động trước đó — không lặp lại trong lần test
thủ công này): 137/137 unit test PASS (bao gồm cả các test cập nhật cho thay đổi
#1 và #2 ở trên), build PASS, 31 test case tích hợp trong docs/TEST_CASES.md.

⚠️ SỰ CỐ HẠ TẦNG (không phải bug code): backend trong môi trường dev nhiều lần
khởi động rất chậm/treo tạm thời do có nhiều session Claude khác (S2, S7...) chạy
song song trên cùng máy, cạnh tranh tài nguyên. Đã xử lý bằng cách khởi động lại
kiên nhẫn mỗi lần, không ảnh hưởng đến kết quả test.

👉 Yêu cầu: Giải thích tính năng Quản lý câu hỏi cho người dùng — đặc biệt nhấn
mạnh 5 điểm cải tiến phát sinh trong lúc test (mục 1-5 ở trên) vì đây là các thay
đổi so với bản thiết kế gốc ban đầu của S1. Hỏi xem người dùng có thắc mắc gì không.
