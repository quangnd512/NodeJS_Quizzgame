[TỪ S3-SOATLOI]

↩️ ĐÃ SỬA XONG (làm lại lần 1): resolveReport() thiếu claim-pattern race condition
🌿 BRANCH: feature/question-management-hub

⚠️ LƯU Ý QUAN TRỌNG: Khi bắt đầu sửa, phát hiện nhiều session khác đang chạy song song
trên CÙNG thư mục làm việc (không phải worktree riêng) và đã để lại thay đổi CHƯA COMMIT
ngay trong chính các file tôi cần sửa (practice.service.ts, practice.errors.ts — S2 đã
thêm sẵn logic "resubmit report" + "faceted filter" chưa commit). Để tránh gộp nhầm/che
khuất công việc dang dở của session khác, tôi CHỈ sửa đúng phần được yêu cầu, KHÔNG đụng
phần khác, và CHƯA COMMIT thay đổi của mình — nhờ S8/người dùng quyết định thời điểm commit
phù hợp (có thể gộp cùng lúc S7 đóng gói, sau khi các session khác commit xong phần của họ).

📋 ĐÃ SỬA (đúng theo hướng dẫn chi tiết của S8 trong PENDING/S3.md):
1. Thêm `ReportNotPendingError extends PracticeError` (HTTP 409, mã REPORT_NOT_PENDING) vào
   practice.errors.ts — theo đúng convention SubmissionNotPendingError.
2. Map REPORT_NOT_PENDING: 409 vào ERROR_CODE_TO_HTTP_STATUS trong app.ts.
3. Sửa resolveReport(): thêm claim report CHÍNH bằng
   tx.questionReport.updateMany({ where: { id: reportId, status: 'PENDING' }, data: { status } })
   NGAY SAU khi đọc currentQuestion, TRƯỚC bước tạo snapshot/update Question. Nếu
   claimed.count === 0 → throw ReportNotPendingError, transaction rollback ngay (không tạo
   snapshot/update Question/batch-resolve report khác cho lần gọi thua cuộc). Xoá dòng
   tx.questionReport.update đứng riêng cũ (claim đã làm thay).
4. Bổ sung 1 test mới trong practice.service.test.ts: mock updateMany (claim) trả {count:0}
   → assert throw ReportNotPendingError, assert question.update/questionEditHistory.create/
   questionReport.findMany (bước batch) đều KHÔNG được gọi.
5. Cập nhật 5 test cũ trong cùng describe('resolveReport') để khớp claim pattern mới.
6. Sửa docs/CODE_REVIEW_LOG.md: đính chính dòng ghi nhận sai ở vòng đầu (tiêu chí Edge cases,
   dòng "report đã bị resolve trước đó" — lúc đó nhầm với hành vi updateReport() cũ) + thêm
   mục "Làm lại lần 1" mô tả đầy đủ lỗi + cách sửa.
7. Thêm test case #32 vào docs/TEST_CASES.md (mục Feature 014).

🧪 KẾT QUẢ TEST TỰ ĐỘNG:
- practice.service.test.ts riêng: 11/11 PASS (10 test cũ đã cập nhật mock + 1 test mới)
- Toàn bộ suite backend: 138/138 PASS (137 trước đó + 1 test mới)
- Build (tsc): KHÔNG kiểm chứng lại được lần này — máy bị nhiều session Claude Code khác chạy
  song song chiếm tài nguyên nặng (lệnh build thường mất vài giây đã treo >5 phút, 0% CPU).
  Dựa vào việc thay đổi chỉ tái sử dụng đúng API Prisma (updateMany với where điều kiện status)
  đã build sạch ở pattern hệt vậy (approveSubmission, đã tự kiểm chứng ở vòng review đầu) nên
  đủ tự tin về type-safety. Đề nghị S8 tự chạy lại tsc --noEmit khi máy đỡ tải để xác nhận
  độc lập lần cuối trước khi chuyển S7.

📁 FILES ĐÃ SỬA THÊM (CHƯA COMMIT — xem lưu ý ở trên):
- backend/src/services/practice/practice.errors.ts
- backend/src/app.ts
- backend/src/services/practice/practice.service.ts
- backend/src/services/practice/__tests__/practice.service.test.ts
- docs/CODE_REVIEW_LOG.md
- docs/TEST_CASES.md

📌 KHÔNG động vào: batch-resolve report khác (đã đúng từ trước), submission claim pattern,
CAS usage-points, migration, resubmit logic, faceted filter — đúng theo yêu cầu S8.

👉 Nhờ S8 rà soát lại lần cuối (đặc biệt xác nhận build tsc khi máy đỡ tải) trước khi quyết
định bước tiếp theo.
