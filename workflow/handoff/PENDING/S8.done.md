[TỪ S5-THUNGHIEM]

[S5-ThuNghiem] ✅ ĐÃ TEST LẠI XONG: Click-to-Navigate khi bấm thông báo (commit 293b40d)
🌿 BRANCH: feature/notifications

📊 KẾT QUẢ 4 KỊCH BẢN YÊU CẦU (test thủ công thật trên app đang chạy qua trình duyệt):

| # | Kịch bản | Kỳ vọng | Kết quả |
|---|----------|---------|---------|
| 1 | Bấm thông báo STREAK_MILESTONE trong panel | Đánh dấu đã đọc + chuyển sang màn hình Tiến độ (ProgressPage) | ✅ PASS |
| 2 | Bấm thông báo RANK_UP/RANK_DOWN | Đánh dấu đã đọc + chuyển sang màn hình Bảng xếp hạng (LeaderboardPage) | ✅ PASS |
| 3 | Bấm thông báo NEW_EXAM_PAPER | Đánh dấu đã đọc + chuyển sang màn hình Thi thử (ExamPage, danh sách chọn môn) | ✅ PASS |
| 4 | Bấm thông báo REPORT_RESOLVED (targetScreen=null) | CHỈ đánh dấu đã đọc, KHÔNG chuyển màn hình | ✅ PASS — xác nhận panel vẫn mở nguyên tại chỗ, không "pass giả" |

Cả 4 case đều verify bằng cả 2 lớp: (1) quan sát trực tiếp UI qua screenshot browser thật —
không chỉ đọc code, và (2) query DB xác nhận `isRead=true` sau khi bấm cho cả 4 bản ghi.

📋 ĐÃ BỔ SUNG: 4 test case N1-N4 vào `docs/TEST_CASES.md` (mục Feature 013, sau phần Happy
Path, trước Edge Cases) — mô tả đúng hành vi click-to-navigate, để lần sau không bị thiếu
coverage như S8 đã lưu ý.

⚠️ GHI CHÚ HẠ TẦNG (không phải bug code): backend trong môi trường dev bị treo/khởi động rất
chậm nhiều lần trong lúc test (nghi do project nằm trong thư mục Desktop được đồng bộ iCloud,
khiến file bị "chạm" và có lúc treo xác minh module native `fsevents`). Đã xử lý bằng cách khởi
động lại kiên nhẫn, không ảnh hưởng đến kết quả test — nhưng nếu tình trạng lặp lại ở các vòng
sau, có thể cần tắt đồng bộ iCloud cho thư mục dự án hoặc chuyển ra ngoài Desktop.

👉 Yêu cầu: Nhờ S8 xác nhận ĐẠT quality gate lần cuối cho Feature 013.
