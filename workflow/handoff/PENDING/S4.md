[TỪ S3-SOATLOI]

[S3-SoatLoi] ✅ REVIEW + TEST XONG: Khung Free/Premium (Feature 015)
🌿 BRANCH: feature/premium-framework
📌 Commit sửa của S3: 00b7991

📋 KẾT QUẢ REVIEW (7 tiêu chí + API contract):
- Lỗi tìm thấy: 2 race condition (mức nghiêm trọng)
  1. premium.service.ts — grantPremiumMonths nhận sẵn `user` đã fetch từ ngoài, check-rồi-ghi
     không nguyên tử → 2 admin cấp Premium gần như đồng thời có thể làm MẤT HOÀN TOÀN 1 trong 2
     lần cấp (lost update). Sửa bằng CAS (compare-and-swap) retry: đổi signature thành
     grantPremiumMonths(userId, months), tự fetch lại bên trong vòng lặp, updateMany có điều
     kiện WHERE khớp đúng premiumExpiresAt+premiumSince vừa đọc, tối đa 5 lần thử.
  2. users.route.ts — token mở khoá quảng cáo (ad-unlock) của Free bị tiêu thụ TRƯỚC KHI validate
     xong subjects (số lượng 1-7, mã môn hợp lệ, không trùng) → request sai định dạng vẫn "đốt"
     token oan, Free phải xem lại quảng cáo dù chưa đổi môn thành công. Sửa bằng cách đổi thứ tự:
     validate đầy đủ trước, tiêu thụ token sau.
- 7 tiêu chí còn lại: transaction ✅, error handling ✅, input validation ✅, N+1/index ✅
  (index users(premiumExpiresAt) đúng cho cron), không có any ✅, edge cases ✅ (đặc biệt thuật
  toán streak-freeze với "trailing forgiveness" đã kiểm chứng kỹ), API contract khớp
  docs/api/drafts/free-premium-framework.yaml ✅

🧪 KẾT QUẢ TEST TỰ ĐỘNG:
- Unit test: 201/201 PASS (193 của S2 + 8 mới của S3)
- Build: PASS (Backend tsc sạch, Frontend tsc -b && vite build sạch, 1m43s)
- npm audit: không kiểm tra lại riêng (package.json không đổi, không thêm dependency mới)

🔍 ĐỐI CHIẾU THIẾT KẾ S1: Đúng API contract, đúng DB schema (User +3 field, model AppSettings
mới, enum +2 giá trị), 16/16 TASK hoàn thành đúng mục tiêu.

📌 Ghi chú (không phải lỗi, chỉ quan sát): Admin chưa có cách xem trạng thái Premium hiện tại
của user trong bảng danh sách/chi tiết (AdminUserListItem/AdminUserDetail chưa có field
isPremium/premiumExpiresAt) — chỉ thấy qua toast lúc tự tay cấp. API draft không yêu cầu việc
này nên KHÔNG coi là thiếu sót của PR — chỉ là gợi ý cải tiến UX cho vòng sau nếu cần.

📁 FILES S3 ĐÃ SỬA/THÊM (commit 00b7991):
- backend/src/services/premium/premium.errors.ts — thêm PremiumUserNotFoundError, PremiumGrantConflictError
- backend/src/services/premium/premium.service.ts — CAS retry cho grantPremiumMonths
- backend/src/services/premium/__tests__/premium.service.test.ts — sửa toàn bộ test theo signature mới + 3 test mới
- backend/src/services/admin-users/admin-users.service.ts — cập nhật lời gọi grantPremiumMonths(userId, months)
- backend/src/services/admin-users/__tests__/admin-users.service.test.ts — sửa 1 assertion
- backend/src/app.ts — map PREMIUM_USER_NOT_FOUND: 404, PREMIUM_GRANT_CONFLICT: 409
- backend/src/services/users/users.service.ts — validateAndNormalizeSubjects đổi private→public
- backend/src/services/users/__tests__/users.service.test.ts — thêm 5 test
- backend/src/routes/users.route.ts — đổi thứ tự validate trước khi tiêu thụ token ad-unlock
- docs/TEST_CASES.md — thêm 26 test case Feature 015
- docs/CODE_REVIEW_LOG.md — review đầy đủ theo 8 tiêu chí

👉 Yêu cầu: Viết/cập nhật tài liệu đầy đủ cho tính năng này (CHANGELOG, user-guide, API docs
chính thức đối chiếu với docs/api/drafts/free-premium-framework.yaml — lưu ý file draft này
trước đó S2 để lại CHƯA commit, giờ đã có trong docs/api/drafts/, v.v.)
