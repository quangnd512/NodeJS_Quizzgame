[TỪ S3-SOATLOI]

[S3-SoatLoi] ✅ REVIEW + TEST XONG: Quản lý câu hỏi (Học sinh đóng góp câu hỏi + Thiết kế lại luồng xử lý báo cáo câu hỏi)
🌿 BRANCH: feature/question-management-hub
📌 Commit sửa của S3: ffba1a0

📋 KẾT QUẢ REVIEW (7 tiêu chí + API contract):
- Lỗi tìm thấy: 3 race condition (mức nghiêm trọng — liên quan điểm thưởng)
- Đã sửa:
  1. approveSubmission — chống 2 admin duyệt trùng 1 submission (tạo trùng QuestionBank +
     thưởng điểm 2 lần) → thêm bước "claim" bằng updateMany({status:'PENDING'}) đầu transaction
  2. rejectSubmission/updateSubmission/deleteSubmission — chống sửa/xoá/từ chối "hụt" khi
     submission vừa bị xử lý xong → đổi sang updateMany/deleteMany có điều kiện status
  3. question-bank.service.ts usage-points trigger — lost-update trên usagePointsEarned khi
     2 lần thêm cùng câu vào đề thi gần như đồng thời (có thể vô hiệu hoá trần 100đ/câu) →
     sửa bằng compare-and-swap có retry
- 7 tiêu chí còn lại: transaction ✅, error handling ✅, input validation ✅, N+1/index ✅
  (1 ghi chú nhẹ, chấp nhận được), không có `any` ✅, edge cases ✅, API contract khớp
  docs/api/drafts/quan-ly-cau-hoi.yaml ✅

🧪 KẾT QUẢ TEST TỰ ĐỘNG:
- Unit test: 137/137 PASS (117 của S2 + 20 mới của S3)
- 2 file test mới hoàn toàn: question-bank.service.test.ts, text-similarity.utils.test.ts
- Build: PASS (Backend tsc sạch, Frontend tsc -b && vite build sạch)
- Lint: không chạy được trong sandbox này (eslint bị treo do tranh chấp tài nguyên với 2
  session khác đang chạy song song) — bù bằng tsc strict (noUnusedLocals/noUnusedParameters)
  sạch + rà thủ công toàn diff, không phát hiện vi phạm
- npm audit --audit-level=high: các lỗ hổng đều pre-existing, không có dependency mới trên branch

🔍 ĐỐI CHIẾU THIẾT KẾ S1: Đúng API contract, đúng DB schema, cả 12/12 TASK hoàn thành đúng
mục tiêu. Đã xác nhận hợp lý 5 điểm S2 nêu trong "LƯU Ý CHO SESSION 3" (difficulty mặc định=2,
usage points chỉ hook addFromBank, updateReport() cũ giữ nội bộ, migration thủ công, chưa có
FE test suite — đúng pattern hiện tại của repo).

📁 FILES S3 ĐÃ SỬA/THÊM (commit ffba1a0):
- backend/src/services/submission/submission.service.ts
- backend/src/services/submission/__tests__/submission.service.test.ts
- backend/src/services/exam/question-bank.service.ts
- backend/src/services/exam/__tests__/question-bank.service.test.ts (mới)
- backend/src/utils/__tests__/text-similarity.utils.test.ts (mới)
- docs/TEST_CASES.md (+31 test case)
- docs/CODE_REVIEW_LOG.md (review đầy đủ theo 8 tiêu chí)

👉 Yêu cầu: Viết/cập nhật tài liệu đầy đủ cho tính năng này (CHANGELOG, user-guide, API docs
chính thức đối chiếu với docs/api/drafts/quan-ly-cau-hoi.yaml, v.v.)
