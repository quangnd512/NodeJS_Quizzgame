[TỪ S3-SOATLOI]

✅ REVIEW + TEST XONG: Đợt 1a — Nền móng App Di Động (Mobile Foundation)
🌿 BRANCH: feature/mobile-foundation

📋 KẾT QUẢ REVIEW (8 tiêu chí):
- Tiêu chí 1 (Atomic transaction), 2 (Race condition), 5 (N+1/Index): N/A — feature thuần
  wiring UI/native-SDK, không đụng DB/backend (backend hoàn toàn không đổi, đã xác nhận qua
  git diff master..HEAD chỉ có file trong mobile/ + 1 file docs).
- Tiêu chí 3 (Error handling): 1 lỗi nhỏ tìm thấy & đã sửa — mobile/src/api/hello.ts thiếu
  try/catch quanh JSON.parse(), không nhất quán với parseJsonBody() trong client.ts.
- Tiêu chí 4 (Input validation): Pass.
- Tiêu chí 6 (TypeScript any): Pass — grep toàn bộ mobile/src + App.tsx không có any nào.
- Tiêu chí 7 (Edge cases): Pass — huỷ đăng nhập Google/Apple giữa chừng, Apple fullName chỉ
  có ở lần đăng nhập đầu, thiếu Play Services, JWT hết hạn giữa lúc dùng app (401 tự đăng
  xuất qua listener pattern), admin secret cũ hết hiệu lực lúc boot, mất mạng — đều xử lý
  đúng, không có đường crash không kiểm soát.
- Tiêu chí 8 (API contract): 1 điểm lệch tìm thấy & đã sửa — docs/api/drafts/mobile-foundation.yaml
  ghi sai shape POST /api/users/subjects (request: {subjects: string[]}) trong khi contract
  THẬT (backend/src/routes/users.route.ts) là {subjects: [{id, name?}]} — code mobile
  (api/users.ts) đã implement ĐÚNG theo thực tế, chỉ sửa lại draft yaml cho khớp.
- Dọn thêm: 6 warning lint (unused `err` trong catch ở client.ts×3/hello.ts×1,
  import/no-duplicates ở App.tsx×2) — đã sửa cả 4 vị trí.

🧪 KẾT QUẢ TEST TỰ ĐỘNG:
- KHÔNG viết test tự động — quyết định có cân nhắc (không phải bỏ sót), lý do đầy đủ trong
  docs/CODE_REVIEW_LOG.md: toàn bộ 17 dòng DoD của S1 được khung theo hướng xác minh THỦ CÔNG
  trên thiết bị/giả lập thật (không có dòng nào yêu cầu test tự động); mobile/ chưa có hạ
  tầng test (jest-expo); logic rủi ro cao nhất (SecureStore mã hoá, nonce chống replay Apple
  Sign-In, tách biệt hoàn toàn Admin/học sinh, listener tự đăng xuất 401) đã đọc kỹ code xác
  nhận đúng. Khuyến nghị dựng jest-expo từ đợt 1b khi có logic nghiệp vụ thật (điểm số...).
- Build: `node node_modules/typescript/bin/tsc --noEmit` PASS sạch (2 lần độc lập).
- Lint: lần chạy sạch ĐẦU TIÊN (trước khi sửa) phát hiện đúng 6 warning, 0 lỗi → đã sửa cả 4
  vị trí bằng fix chuẩn, cơ học. KHÔNG re-verify được bằng 1 lần chạy sạch sau khi sửa — 6 lần
  thử lại (kể cả sau khi đóng toàn bộ 7 session Claude khác đang chạy song song theo yêu cầu
  người dùng, loại trừ hẳn nguyên nhân tranh chấp tài nguyên) đều gặp lỗi môi trường
  `ETIMEDOUT` khi ESLint load plugin (tsc không gặp hiện tượng này, chạy ổn định) — đã tự đọc
  lại kỹ code xác nhận đúng cú pháp/đúng loại fix, người dùng đã xác nhận chấp nhận kết quả
  này. Chi tiết đầy đủ trong CODE_REVIEW_LOG.md.
- npm audit (--audit-level=high): 20 lỗ hổng (11 moderate, 9 high) — đã soát kỹ từng chuỗi phụ
  thuộc, TẤT CẢ thuộc toolchain build-time (nội bộ eslint / @expo/cli-config-plugins qua
  brace-expansion, uuid), không chạy trong app đã build, không có fix hợp lý mà không hạ cấp
  breaking (expo 46 cũ hơn nhiều SDK 57 đang dùng) — chấp nhận rủi ro pre-existing.

🔍 ĐỐI CHIẾU THIẾT KẾ S1: Đúng API contract (sau khi sửa draft), KHÔNG đụng frontend/backend
đúng yêu cầu, đọc code xác nhận đúng cả 17 dòng DoD ở mức tĩnh (state machine AuthContext/
AdminAuthContext, RootNavigator ưu tiên Admin, 5 tab đúng tên, MIN/MAX_SUBJECTS khớp backend,
SecureStore cho JWT/secret/theme, nonce Apple, ẩn nút Apple trên Android, README đầy đủ). Phần
hành vi RUNTIME thật trên thiết bị (không tự kiểm chứng được trong sandbox này) để lại cho S5
test thủ công theo 18 kịch bản đã bổ sung vào docs/TEST_CASES.md (mục "Mobile Foundation").

📁 FILES ĐÃ THAY ĐỔI THÊM (bởi S3):
- mobile/src/api/client.ts, mobile/src/api/hello.ts, mobile/App.tsx (sửa lỗi + dọn lint)
- docs/api/drafts/mobile-foundation.yaml (sửa 1 điểm lệch contract)
- docs/TEST_CASES.md (thêm mục "Mobile Foundation", 18 kịch bản test thủ công)
- docs/CODE_REVIEW_LOG.md (review đầy đủ)

👉 Yêu cầu: Viết/cập nhật tài liệu đầy đủ cho tính năng này (đây là module mobile/ ĐẦU TIÊN
của dự án — có thể cần thêm 1 mục riêng trong docs tổng quan giới thiệu kiến trúc mobile, bên
cạnh cập nhật CHANGELOG/FEATURE_LOG/GLOSSARY/hướng dẫn như thường lệ).
