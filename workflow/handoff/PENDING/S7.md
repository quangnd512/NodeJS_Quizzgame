[TỪ S8-GIAMSAT]

✅ ĐẠT QUALITY GATE: Khung Free/Premium (Feature 015)
🌿 BRANCH: feature/premium-framework

📋 TÓM TẮT: 16/16 TASK hoàn thành đúng thiết kế S1 — feature phức tạp nhất từ trước tới nay
(thuật toán streak-freeze, CAS retry, cache invalidate-on-write, cron, gate nhiều tầng). Trải
qua review S3 (2 race condition sửa: CAS đa-field cho grantPremiumMonths, thứ tự validate/
consume token ad-unlock) và 1 thay đổi phạm vi hợp lý từ S5 (gate thêm 2 field "Xu hướng điểm"
+ "Thống kê theo môn" ở trang Tiến độ, chặn đúng ở backend trả mảng rỗng cho Free).

✅ ĐÂY LÀ LẦN ĐẦU TIÊN 1 FEATURE ĐẠT QUALITY GATE NGAY VÒNG ĐẦU — không cần trả lại session nào.

✅ KẾT QUẢ CUỐI (S8 tự kiểm chứng độc lập, đọc code trực tiếp, không chỉ tin báo cáo):
  - Build: `tsc --noEmit` PASS cả backend + frontend (verify 2 lần độc lập, phải kill+retry do
    máy bị treo I/O — môi trường sandbox, không phải lỗi code, đã xảy ra nhiều lần trước đây)
  - Unit test backend: 201/201 PASS (verify 2 lần độc lập)
  - Lint: `eslint` frontend bị treo I/O vô thời hạn (đã thử 4 lần, kể cả lint 1 file duy nhất) —
    đúng y hệt hiện tượng S3 từng ghi nhận ở Feature 014 (giới hạn sandbox). Chấp nhận kết quả
    "sạch" đã được S2+S3 độc lập xác nhận trước đó, bù bằng `tsc` strict (noUnusedLocals/
    noUnusedParameters) đã PASS.
  - npm audit: lỗ hổng đều pre-existing, không thêm dependency mới
  - Đọc kỹ code các vùng rủi ro cao nhất (S1 tự đánh giá là rủi ro kỹ thuật cao nhất):
    • `computeStreaksWithFreeze` (bridge gap đúng 1 ngày + "trailing forgiveness") — thuật toán
      đúng, có 13 test case riêng bao phủ đủ mọi biên (hết thẻ, trailing 1 ngày/2 ngày, premiumSince
      nằm giữa lịch sử, freezeGrant=0 cho Free, ngày không sắp xếp/trùng lặp)
    • CAS retry đa-field trong `grantPremiumMonths` — đúng, WHERE khớp cả premiumExpiresAt VÀ
      premiumSince, tối đa 5 lần thử, throw PremiumGrantConflictError khi cạn
    • Thứ tự validate-trước-consume token ad-unlock trong `POST /api/users/subjects` — đúng
    • 3 gate Premium (Ôn câu sai, Lịch sử thi thử, 2 field mới ở Tiến độ) — đều chặn đúng Ở
      BACKEND (403 hoặc mảng rỗng tuỳ endpoint), không chỉ ẩn UI
    • Cron cảnh báo hết hạn (3:05 AM, cạnh cron cleanup có sẵn 3:00 AM) — đúng
    • Migration + schema.prisma khớp hoàn toàn; `addMonthsUtc` clamp cuối tháng đúng chuẩn
    • 2 lớp error `ADMIN_USER_NOT_FOUND` (pre-check ở admin-users.service) vs
      `PREMIUM_USER_NOT_FOUND` (re-check bên trong CAS loop) — xác nhận là defense-in-depth hợp
      lý cho 2 điểm vào khác nhau, KHÔNG phải trùng lặp/bug
  - Tài liệu: FEATURE_LOG, GLOSSARY, LESSONS_LEARNED, CHANGELOG, openapi.yaml (bump 1.6.0, nhân
    tiện sửa 12 endpoint bị lồng sai cấu trúc từ trước), admin-guide, user-guide, troubleshooting,
    ADR 012 — đầy đủ
  - docs/TASKS.md: dòng 015 → ✅ Done (2026-07-19)
  - docs/PROJECT_OVERVIEW.md: thêm module "Khung Free/Premium" + chú thích gate mới ở 3 module
    liên quan (Progress Dashboard, Ôn câu sai, Admin User Management)

📦 TÌNH TRẠNG COMMIT: Tốt hơn nhiều so với vòng Feature 014 — phần lớn code đã được commit theo
từng TASK (16 commit riêng biệt + 1 commit fix của S3). Chỉ còn CHƯA COMMIT: thay đổi của S5
(gate 2 field progress.service.ts + App.tsx) và toàn bộ tài liệu (FEATURE_LOG, GLOSSARY, CHANGELOG,
openapi.yaml, admin-guide, user-guide, troubleshooting, ADR 012, docs/TASKS.md, PROJECT_OVERVIEW.md
vừa cập nhật). Đề xuất chia 2 commit: 1 "feat" cho phần S5 bổ sung + 1 "docs" cho toàn bộ tài liệu.

👉 Yêu cầu: Commit đầy đủ (2 commit như trên hoặc theo bạn đánh giá hợp lý hơn), push branch
feature/premium-framework lên GitHub, kiểm tra CI pass, hỏi người dùng merge.
