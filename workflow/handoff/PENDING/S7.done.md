[TỪ S8-GIAMSAT]

✅ ĐẠT QUALITY GATE: Quản lý câu hỏi — Học sinh đóng góp câu hỏi + Thiết kế lại báo cáo (Feature 014)
🌿 BRANCH: feature/question-management-hub

📋 TÓM TẮT: 12/12 TASK hoàn thành đúng thiết kế S1 + 5 cải tiến hợp lý phát sinh trong lúc S5 test
thủ công (đã giải thích cho người dùng ở vòng S6, người dùng không thắc mắc gì thêm):
  1. Bỏ trường "Chương", thêm chọn dạng câu hỏi (MCQ_4/TRUE_FALSE_4/FILL_BLANK)
  2. Cho phép báo cáo lại 1 câu sau khi report cũ đã xử lý xong (partial unique index)
  3. Lỗi vượt rate-limit hiện dạng modal
  4. Cảnh báo trùng lặp hiện kèm nội dung đầy đủ
  5. Lọc liên động 3 dropdown (status/subject/reason) — endpoint /reports/facets mới

↩️ Có 1 vòng làm lại: S3 bỏ sót claim-pattern chống double-resolve trong `resolveReport()` (cùng
lớp race condition mà chính S3 vừa fix ở 2 chỗ khác trong PR này — approveSubmission, usage-points
CAS) — đã sửa xong, thêm `ReportNotPendingError` (409), 138/138 test PASS.

✅ KẾT QUẢ CUỐI (S8 tự kiểm chứng độc lập nhiều lần, không chỉ dựa báo cáo):
  - Build: `tsc --noEmit` PASS cả backend + frontend
  - Lint: `eslint` frontend PASS (sạch)
  - Unit test backend: 138/138 PASS
  - npm audit: lỗ hổng đều pre-existing (xlsx, firebase-admin/google-gax chain), không thêm
    dependency mới trên branch này
  - 2 migration mới đã đọc kỹ, khớp `schema.prisma`: `20260713100000_submission_question_types`,
    `20260714000000_report_resubmit_after_resolved` (dùng raw SQL cho partial unique index vì
    Prisma schema không khai báo được, có backfill trước khi set NOT NULL — an toàn)
  - Test thủ công: 22/22 PASS (S5) + code review 8 tiêu chí (S3, 2 vòng) đều ĐẠT
  - Tài liệu: FEATURE_LOG, GLOSSARY, LESSONS_LEARNED, CHANGELOG, openapi.yaml (bump 1.5.0),
    admin-guide, user-guide, troubleshooting, ADR 011 — đầy đủ; S8 tự bổ sung 1 dòng test case
    #32 còn thiếu trong TEST_CASES.md (nội dung đã có sẵn trong CODE_REVIEW_LOG.md)
  - docs/TASKS.md: dòng 014 → ✅ Done (2026-07-15)
  - docs/PROJECT_OVERVIEW.md: cập nhật mô tả module báo cáo (redesign) + thêm module "Đóng góp
    câu hỏi (Học sinh)"

⚠️⚠️ RẤT QUAN TRỌNG — TÌNH TRẠNG COMMIT: Toàn bộ nhánh này hiện có RẤT NHIỀU thay đổi CHƯA COMMIT
trong working tree, tích luỹ qua 3 vòng khác nhau (không phải lỗi của bạn, là do nhiều session
Claude chạy song song trên CÙNG 1 working directory thay vì git worktree riêng — đã được S2 và S3
ghi nhận, và S1 cũng lưu ý y hệt trong kế hoạch gốc). Cụ thể:
  - Code KHÔNG có gốc commit sạch: nhiều thay đổi từ vòng S5 test (resubmit logic, faceted
    filter, question types — đã có migration riêng) VÀ vòng S3 làm lại (race condition
    resolveReport) đều đang nằm thẳng trong working tree, CHƯA commit lên bất kỳ commit nào.
  - Toàn bộ tài liệu (FEATURE_LOG, GLOSSARY, CHANGELOG, v.v.) cũng chưa commit.
  - Trước khi push, bạn cần tự quyết định cách chia commit hợp lý (có thể theo mẫu các vòng
    trước: 1 commit "feat" cho phần S5 bổ sung + 1 commit "fix" cho phần S3 sửa race condition +
    1 commit "docs" cho tài liệu — hoặc gộp lại tuỳ bạn đánh giá), miễn đảm bảo lịch sử git rõ
    ràng và message mô tả đúng nội dung. Chạy lại `git status`/`git diff` kỹ trước khi commit vì
    danh sách file khá dài.
  - Nhắc người dùng cân nhắc dùng `git worktree` riêng cho các session chạy song song ở các vòng
    tính năng sau, để tránh lặp lại tình trạng này (đã được S1/S2/S3 ghi nhận nhiều lần).

👉 Yêu cầu: Commit đầy đủ (chia commit hợp lý như trên), push branch feature/question-management-hub
lên GitHub, kiểm tra CI pass, hỏi người dùng merge.
