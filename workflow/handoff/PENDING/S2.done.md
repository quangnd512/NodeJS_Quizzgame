[TỪ S1-KIENTRUCSU]

🎯 TÍNH NĂNG CẦN LÀM: Exam UX Improvements (Feature 012)
🌿 BRANCH: feature/exam-ux-improvements

📝 TÓM TẮT YÊU CẦU NGƯỜI DÙNG:
Thêm 3 cải tiến trải nghiệm thi thử:
1. Lỡ thoát app → mở lại → hỏi "tiếp tục không?"; nếu hết giờ → tự nộp bài
2. Trong bài thi có nút "Thoát" với hộp xác nhận trước khi huỷ
3. Sau khi thoát (huỷ bài) → có thể thi môn khác ngay, không bị chặn

🔧 CHI TIẾT KỸ THUẬT:

Files ảnh hưởng:
- backend/prisma/schema.prisma — thêm ABANDONED vào ExamSessionStatus enum
- backend/src/services/exam/exam.service.ts — thêm getActiveSession(), abandonSession()
- backend/src/routes/exam.route.ts — thêm 2 endpoint mới
- frontend/src/App.tsx — resume dialog, localStorage auto-save, exit button

DB thay đổi:
- Thêm giá trị ABANDONED vào enum ExamSessionStatus
- Cần chạy migration mới

API mới:
- GET /api/exam/active → trả { session: {..., remainingSeconds} } hoặc { session: null }
- POST /api/exam/:id/abandon → { success: true } hoặc 403/404/409
- Chi tiết xem: docs/api/drafts/exam-ux-improvements.yaml

Logic frontend quan trọng:
- Lưu đáp án tạm: localStorage key = exam_draft_{sessionId}, value = { [examQuestionId]: selectedAnswer }
- Cập nhật mỗi khi user chọn đáp án (không cần debounce, nhẹ thôi)
- Khi resume: load từ localStorage, populate lại UI
- Khi hết giờ lúc resume: gọi submit với đáp án từ localStorage; nếu backend từ chối (quá grace 30s) → thông báo "Bài thi đã hết giờ" rồi abandon session

📋 DANH SÁCH TASK:

TASK 1: DB migration — thêm ABANDONED vào ExamSessionStatus enum
        Output: schema.prisma cập nhật + migration file mới
        Phụ thuộc: không

TASK 2: Backend — getActiveSession(userId) + GET /api/exam/active
        Output: endpoint trả về session IN_PROGRESS kèm remainingSeconds, hoặc null
        Phụ thuộc: TASK 1

TASK 3: Backend — abandonSession(userId, sessionId) + POST /api/exam/:id/abandon
        Output: endpoint đổi status → ABANDONED; 403 nếu không phải của mình; 409 nếu không IN_PROGRESS
        Phụ thuộc: TASK 1

TASK 4: Frontend — Tự lưu đáp án vào localStorage mỗi khi chọn
        Output: exam_draft_{sessionId} cập nhật realtime trong localStorage
        Phụ thuộc: không

TASK 5: Frontend — Resume flow khi vào màn hình thi
        Logic:
        - Gọi GET /api/exam/active trước khi show exam UI
        - Nếu có session còn giờ → hỏi "tiếp tục không?"; Có → load đáp án từ localStorage + vào bài; Không → gọi abandon rồi thi bình thường
        - Nếu có session hết giờ → tự submit với đáp án từ localStorage → hiện kết quả (hoặc thông báo hết hạn nếu backend từ chối)
        - Nếu không có session → thi bình thường
        Output: dialog + logic khôi phục hoàn chỉnh
        Phụ thuộc: TASK 2, TASK 3, TASK 4

TASK 6: Frontend — Nút "Thoát" trong màn hình thi + hộp xác nhận
        Logic: nút "Thoát" → dialog "Bạn có chắc muốn thoát? Bài thi sẽ bị huỷ." → xác nhận → gọi POST /api/exam/:id/abandon → navigate ra ngoài
        Output: nút thoát hoạt động đúng, không thoát nếu nhấn huỷ
        Phụ thuộc: TASK 3

📋 DEFINITION OF DONE:
□ GET /api/exam/active → trả về session đang dở kèm remainingSeconds chính xác
□ GET /api/exam/active → trả null khi không có session nào IN_PROGRESS
□ POST /api/exam/:id/abandon → 200, session đổi thành ABANDONED
□ POST /api/exam/:id/abandon của session không thuộc user → 403
□ Sau khi ABANDON → POST /api/exam/start bất kỳ môn → không còn bị 409
□ Vào màn hình thi khi có session dở còn giờ → hiện dialog hỏi tiếp tục
□ Chọn "Tiếp tục": đồng hồ đúng thời gian còn lại, đáp án cũ được khôi phục
□ Chọn "Huỷ" từ dialog: session bị abandon, có thể thi môn khác ngay
□ Vào khi hết giờ: tự động nộp → hiện kết quả (hoặc thông báo hết hạn nếu quá grace)
□ Trong bài: nút "Thoát" → hộp xác nhận → xác nhận → abandon → ra ngoài
□ Không xác nhận thoát → ở lại làm tiếp bình thường
□ Mỗi lần chọn đáp án → tự lưu vào localStorage
□ Build BE + FE không lỗi, unit test không giảm

📄 API DRAFT: docs/api/drafts/exam-ux-improvements.yaml

✅ YÊU CẦU:
- Checkout branch: feature/exam-ux-improvements
- Thực hiện lần lượt TASK 1 → 6
- Khi xong, tổng kết công việc và báo Session 3 review

⚠️ LƯU Ý ĐẶC BIỆT:
- abandonSession() phải idempotent-safe: gọi lại khi đã ABANDONED → 409 (không crash)
- getActiveSession() chỉ trả IN_PROGRESS, không trả ABANDONED hay COMPLETED
- remainingSeconds có thể âm nếu hết giờ — frontend cần xử lý cả trường hợp này
- Anti-Cheat (Feature 011): startExam() hiện chặn nếu có IN_PROGRESS cùng môn → sau khi ABANDON phải không còn bị chặn (bao gồm cả môn khác, không chỉ môn cũ)
- Đáp án lưu localStorage không cần encrypt — chỉ là draft tạm, không ảnh hưởng tính toán điểm
- Khi submit từ resume (hết giờ): gọi API submit bình thường với đáp án từ localStorage; backend đã có grace 30s
